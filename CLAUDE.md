# CLAUDE.md — Horse Haven of Tennessee Ops Platform

## This Repo Runs Newer Package Versions Than Training Data Usually Covers

Confirmed while scaffolding (July 2026): **Next.js 16.2.10, React 19.2.4, Prisma 7.8.0, @clerk/nextjs ^7.5.18.** Each of these has real breaking changes from the conventions an AI coding assistant is likely to default to from memory. Specific traps already hit once, don't repeat them:

- **Next.js 16:** the middleware file is `src/proxy.ts`, not `src/middleware.ts` (that name is Next.js ≤15 only). Before writing any App Router code, check `node_modules/next/dist/docs/` for this version's actual conventions — a warning to this effect is also in `AGENTS.md` at the repo root, which this file intentionally duplicates since not every tool reads both.
- **Prisma 7:** middleware (`$use`) is gone entirely — Client Extensions (`$extends`) are the only way to hook queries, which is what the ChangeLog mechanism in `src/lib/prisma.ts` relies on. The generator provider is `prisma-client` (not `prisma-client-js`), requires an explicit `output` path, and `PrismaClient` now requires a driver adapter passed to its constructor (`@prisma/adapter-pg` here) — `new PrismaClient()` with no arguments throws. Connection config lives in `prisma.config.ts`, not inline in the schema's `datasource` block. **The seed command also moved** — it's `migrations.seed` inside `prisma.config.ts`, not the `"prisma": {"seed": ...}` block in `package.json` (that field is silently ignored now; don't be fooled into thinking it still does anything). The project's `package.json` has `"type": "module"` because of this whole ESM shift. **Neon specifically needs two separate connection strings** — `DATABASE_URL` (pooled, has `-pooler` in the hostname, used by the app at runtime via the adapter) and `DIRECT_URL` (unpooled, used by `prisma.config.ts` for migrations and by `prisma/seed.ts`). PgBouncer's transaction-pooling mode breaks schema migrations and can cause issues with multi-statement seed scripts, so don't collapse these back into one variable.
- **Watch for a Turbopack + Prisma 7 module resolution error** (`Cannot find module '.prisma/client/default'`) during `npm run dev`. One source describes this as a real conflict between the `prisma-client` generator provider and Next 16's Turbopack dev server, fixable by reverting to `prisma-client-js` with no `output` path plus a `next.config.ts` adjustment. This wasn't reproducible in the sandbox this project was scaffolded in (no live DB to actually run `next dev` against), so the current setup follows official Prisma guidance rather than preemptively working around an unconfirmed issue — but if this exact error shows up, that's the fix to reach for.
- **Clerk v7:** `auth()` is async and returns `isAuthenticated` explicitly — don't rely on truthy `userId` alone to check sign-in state. **`SignedIn`/`SignedOut` no longer exist** — they're replaced by a single `<Show when="signed-in">` / `<Show when="signed-out">` component (also accepts `{ role: "..." }`, `{ permission: "..." }`, etc., or a predicate function), imported from `@clerk/nextjs` same as before. `SignInButton`, `SignUpButton`, `UserButton`, `SignOutButton` are unchanged.
- **Webhook verification:** `verifyWebhook(req)` from `@clerk/nextjs/webhooks` (not manual `svix` package usage) reads `CLERK_WEBHOOK_SIGNING_SECRET` automatically. See `src/app/api/webhooks/clerk/route.ts` for the working pattern — it's already wired to create/link/deactivate `Volunteer` rows on `user.created`/`user.updated`/`user.deleted`. If `src/proxy.ts` ever grows real route protection (it's currently a bare `clerkMiddleware()` with nothing enforced), the webhook route must stay excluded from it — Clerk's webhook POSTs carry no user session, only svix signature headers.

If a future session needs a package bumped further, re-verify against that package's actual current docs before writing code against it from memory — this project got burned once already by assuming Prisma 5/6-era conventions applied.

## A Real Unresolved Risk: Next.js 16.2.6+ Server Actions FormData Regression

Next's May 2026 security release (fixing CVE-2026-23870, a real DoS vulnerability) shipped in 16.2.6 and introduced a separate regression: Server Actions combined with `useActionState` can receive an empty `FormData` object on submit (tracked as vercel/next.js#93754). We're on 16.2.10, four patches past where this was reported, and it's unconfirmed whether a later patch fixed it — this project has no way to verify that from where it was scaffolded.

**Mitigation already in place:** `src/app/checkin/actions.ts` and its form deliberately avoid `useActionState` — plain `<form action={serverAction}>` receiving `FormData` directly, which is outside the exact reproduction case in the bug report (though the underlying cause, described as a change to Flight protocol payload deserialization, could plausibly affect plain Server Actions too — this wasn't confirmed either way). If a form submission results in `formData.get(...)` calls returning `null` for fields that were actually filled in, this regression is the first thing to suspect, not a bug in the handler logic. Check `npm ls next` against the current patched version before spending time debugging application code.

## Read CONTEXT.md First

Read `CONTEXT.md` before starting any new feature or schema change. It has the reasoning behind every table — most alternate designs that look "obvious" were already considered and rejected for a specific reason documented there. This file is the working conventions; `CONTEXT.md` is the why.

## Project Snapshot

Internal operations platform for a working horse rescue, built and maintained as an ongoing volunteer contribution — not a funded project. It needs to keep running on $0/month indefinitely and needs to stay maintainable by one person with limited spare time, possibly handed off eventually to someone less technical. Prioritize simplicity and clarity over cleverness in every decision. If two approaches are roughly equal in effort, take the one that's easier to understand six months from now with no memory of why it was built that way.

## Stack

Next.js (App Router) + TypeScript, Tailwind CSS, Postgres (Neon) + Prisma, Clerk (auth only — not authorization), Cloudflare R2 (file storage), Pusher (chat), Resend (email), Playwright + TypeScript (E2E testing), Vercel + GitHub Actions (CI/CD, cron jobs).

## Non-Negotiable Constraints

- **Stay on free tiers.** Do not add a paid dependency, a paid tier of an existing service, or anything with a per-seat/per-usage cost without flagging it explicitly first. The one intentional exception already agreed to is the possibility of a paid Neon tier purely for backup convenience — everything else stays free by default.
- **No hard deletes on `Animal` or `Volunteer` records**, or anything hanging off them (feeding, medical, weight, pasture history). These move through status fields, never `DELETE`. This is a legal-defensibility requirement, not just a style preference — some animal records may need to hold up as evidence in active custody proceedings.
- **ChangeLog is append-only.** Never write code that edits or deletes an existing `ChangeLog` row. Corrections are new rows.
- **Authorization lives in the database, not Clerk.** Every permission check goes through `Volunteer.role`, not Clerk metadata. Clerk only answers "who is this."

## ChangeLog Implementation

Already built in `src/lib/prisma.ts` — `withChangeLog(base, changedBy, note?)` wraps the base Prisma client in a Client Extension that intercepts `create`/`update` on tracked models and writes field-level diffs automatically. Every server action or route handler that writes to a tracked model should call `withChangeLog(prisma, currentVolunteer.id)` (from `src/lib/auth.ts`'s `requireVolunteer()`) rather than writing to `prisma` directly, and never call `prisma.changeLog.create()` by hand — if you find yourself doing that, the write should go through the extension instead.

Tracked models (per `CONTEXT.md` §4): `Animal`, `Volunteer`, `FeedingBaseline`, `FeedingOverride`, `MedicationRegimen`, `MedicationLog`, `CareEntry`, `HealthIssue`, `WeightEntry`, `AnimalMetric`, `Placement`, `CredentialRecord`, `CheckIn`, `VolunteerTagAssignment`. Add new models to the `trackedModels` array in `src/lib/prisma.ts` when they're introduced, not to a separate list — there is only one source of truth for what's tracked. (`Horse`/`HorseMetric` were the model names before the July 2026 Horse→Animal rename — see the Terminology note near the top of Repo Layout below. Pre-rename `ChangeLog` rows still say `entityType: "Horse"`/`"HorseMetric"` since that was factually the model name when they were written; only new writes use `"Animal"`/`"AnimalMetric"`. `PastureAssignment` was tracked before V2.md Session 1 replaced it with `AnimalLocationAssignment`, which is deliberately **not** tracked — see that model's schema comment; it captures who/when directly on the row instead.)

Logs both `CREATE` (one row per field, `oldValue: null`) and `UPDATE` (field-level diff only, not whole-record snapshots). `updatedAt` is deliberately excluded from diffing since it changes on every write and would just be noise.

## Schema Conventions

- `cuid()` for all IDs.
- camelCase field names, no `@map`/`@@map` to snake_case.
- `Decimal`, never `Float`, for any measured quantity (feed amounts, weights, metrics) — avoids floating-point rounding drift.
- New repeating categorical values (a new feed type, a new care type, a new credential type) are lookup table rows added through the app, not new enum values requiring a migration. Reserve real Prisma enums for things fixed by actual real-world logic (`Role`, `ShiftType`, `HandlingColor`, `AnimalSex`) that won't casually grow.
- Before adding a new table for "something they might want to track later," check whether it actually fits the generic `AnimalMetric` pattern (point-in-time numeric measurement) first — that table exists specifically to absorb future measurement types without a migration.

## Permissions Quick Reference

| Role | Can write | Read scope |
| --- | --- | --- |
| ADMIN | Everything | Everything |
| SHIFT_LEAD | `CareEntry`, `FeedingOverride`, `CheckIn` (their shift), `MedicationLog`, `HealthIssue` (open/resolve), `WeightEntry`, `AnimalMetric`, `AnimalLocationAssignment`, manual Blue release, `VolunteerTagAssignment` (assign/remove) | All shifts/check-ins org-wide |
| VOLUNTEER | Own `CheckIn`, own `CredentialRecord` (self-attestation only) | Own data, shift-relevant feeding/pasture info |
| GUEST | Nothing by default | Time-boxed, read-only |

`SHIFT_LEAD` and `VOLUNTEER` never get write access to `FeedingBaseline`, `Animal` core fields, `Volunteer` records/roles, or `Location`/`AnimalLocationAssignment` type definitions (assigning an animal to an existing location is Admin-or-Shift-Lead per V2.md Session 1; creating a new `Location` itself is Admin-only) — those stay Admin-only regardless of how the UI is framed. `MedicationRegimen` (the standing plan, as opposed to `MedicationLog`'s daily entries) is Admin-only too, same split as `FeedingBaseline`/`FeedingOverride`. `TierThreshold` and `CredentialType` (the annual-compliance-training side of it — see the V2.md Session 2 note in Repo Layout below) are Admin-only config, same lookup-table pattern as `Location`. Manual Blue release (`Volunteer.blueReleasedAt`) is Admin-or-Shift-Lead, matching `AnimalLocationAssignment`'s split — see `src/app/volunteers/tier-actions.ts`. Logging a `CredentialRecord` (self-attestation) is open to any signed-in volunteer, but **only for themselves** — there's no admin-enters-on-someone-else's-behalf path yet, a deliberate MVP scope cut (V2.md Session 2 asks for self-attestation specifically). `VolunteerTag` (the tag *type*, e.g. defining "Go Team" itself, including its eligibility-report threshold) is Admin-only config, same lookup-table pattern as `Location`/`TierThreshold`/`CredentialType` — but *assigning or removing* an existing tag on a volunteer (`VolunteerTagAssignment`) is Admin-or-Shift-Lead, same split as `AnimalLocationAssignment`/`Location` (see V2.md Session 3, `src/app/volunteers/tag-actions.ts`). Never automatic — the "eligibility candidates" report (`src/lib/tags.ts`) is a read-only list for a human to act on, not a gate.

`SHIFT_LEAD` write access to `MedicationLog` and `HealthIssue` was a judgment call made when building those features (not an explicit decision from Lori/Ashley) — extended from the existing `FeedingOverride`/`CareEntry` pattern on the reasoning that shift leads are the ones actually present to give medication and notice new issues, and admins aren't at every shift. Worth confirming for real; see `CONTEXT.md` §16.

`SHIFT_LEAD` write access to `WeightEntry`/`AnimalMetric` (weight, Henneke BCS, height) was an explicit call from James when Metrics was built, not an inference — mirrors the `CareEntry` reasoning (routine observation logged during a shift, e.g. at a weigh-in) rather than the `FeedingBaseline`/`MedicationRegimen` Admin-only pattern.

## Repo Layout — What Already Exists

**Terminology note (July 2026):** the core entity was renamed `Horse` → `Animal` (schema, migration, all routes/actions, both test suites) to accommodate mules, donkeys, minis, ponies, and non-equine animals (a species enum — `HORSE | DONKEY | MULE | MINI_HORSE | PONY | CAT | OTHER` — was added at the same time; every pre-existing record defaulted to `HORSE` except Binx, the barn cat, matched by name and confirmed with James before backfilling to `CAT`). Routes moved from `/horses` to `/animals`; user-visible copy (nav links, page headings, button text) deliberately still says "Horse"/"Horses" since that's what staff call it day to day — only the underlying model, file paths, and route segments changed. The paths below already reflect the renamed state.

- `prisma/schema.prisma` — the full Phase 1 schema.
- `prisma/seed.ts` — seeds lookup tables (`CredentialType` via `upsert` — see the V2.md Session 2 note below on why this one specifically needs to be an upsert, not `createMany`+`skipDuplicates` — plus `FeedType`, `CareType`, `WorkType`, `MetricType`), `TierThreshold` rows for all four tiers, and `Location` rows (`type: FIELD`) for the known field codes. Run via `npm run db:seed`. The RP1–RP6 turnout/bring-in order values in there are a rough approximation from the manual (it lists them as one block, not individually ordered) — confirm the real per-field order before relying on it for anything user-facing.
- `src/lib/prisma.ts` — Prisma client singleton (with the `@prisma/adapter-pg` driver adapter Prisma 7 requires) plus the `withChangeLog` extension.
- `src/lib/auth.ts` — `getCurrentVolunteer()`, `requireVolunteer()`, `requireRole()`. Resolves a Clerk session to the matching `Volunteer` row.
- `src/proxy.ts` — Clerk middleware (Next.js 16 naming).
- `src/app/api/webhooks/clerk/route.ts` — Clerk webhook handler, creates/links/deactivates `Volunteer` rows on user lifecycle events. Matches admin-entered records by email when linking a fresh signup to an existing pre-entered row rather than creating a duplicate.
- `src/app/admin/page.tsx` — first protected page, proves `requireRole` works end to end. Treat this as a diagnostic scaffold, not a real admin dashboard — replace once real admin features exist.
- `src/app/page.tsx` — homepage with Clerk sign-in wired up via `Show`/`SignInButton`/`UserButton`.
- `src/app/checkin/page.tsx` + `src/app/checkin/actions.ts` — first real feature: volunteers log a shift (date, AM/PM, work type, time in/out, notes). Matches the current Google Form's retrospective single-submission flow (both times entered at once), not real-time tap-in/tap-out — see `CONTEXT.md` §8 for why. Deliberately uses a plain `<form action={...}>` Server Action, not `useActionState` — see the Next.js 16 FormData regression note above.
- `src/app/animals/` — Animal core CRUD. `page.tsx` (list, defaults to ACTIVE status only via `?status=all` toggle), `new/page.tsx` + `[id]/page.tsx` + `[id]/edit/page.tsx`, `actions.ts` (`createAnimal`/`updateAnimal`, both Admin-only per the permission table), `AnimalFormFields.tsx` (shared field set between create/edit). `[id]/page.tsx` also renders the photo gallery and upload form.
- `src/lib/r2.ts` + `src/app/api/animals/[id]/photos/route.ts` — photo upload. **Deliberately a Route Handler with a plain `<form method="post" encType="multipart/form-data">`, not a Server Action.** Server Actions cap request bodies at 1MB by default, and the documented `bodySizeLimit` config option has a long history of not reliably working across Next.js versions and hosts (see open GitHub discussions). Current official guidance is explicit: use a Route Handler for file uploads, not a Server Action. Phone camera photos routinely exceed 1MB, so this wasn't optional. Uses `NextResponse.redirect()`, not `redirect()` from `next/navigation` — the latter only works inside the React render pipeline (Server Components/Actions) and throws if called from a plain Route Handler. `AnimalPhoto` is deliberately **not** in the ChangeLog tracked-models list — photos are uploaded once and rarely corrected in place (unlike an animal's status or a check-in time), and the row already captures who/when directly (`uploadedBy`, `takenAt`) without needing field-diff history.
- `src/app/animals/[id]/feeding-actions.ts` — feeding baseline/override, rendered as part of `[id]/page.tsx` rather than a separate route (it's a small enough surface not to warrant its own page). `createFeedingBaseline` is Admin-only; `createFeedingOverride` is Admin or Shift Lead, matching the permission table. "Today's override" is queried per baseline row (`overrides: { where: { date: { gte: today, lt: tomorrow } } } }`) and merged into the same table rather than a separate section — this is the beginning of the "one row per animal showing feed/meds/etc at a glance" dashboard concept from `CONTEXT.md` §8, though the full cross-entity dashboard is still a later slice.
- `src/app/animals/[id]/medication-actions.ts` — same shape as feeding: `createMedicationRegimen` (Admin-only) + `logMedicationAdministered` (Admin or Shift Lead), plus `endMedicationRegimen` (Admin-only, sets `endDate`) since regimens actually end, unlike `FeedingBaseline`. `[id]/page.tsx` only lists regimens with no `endDate` or `endDate >= today`, joined with today's `MedicationLog` the same way feeding joins today's override.
- `src/app/animals/[id]/care-actions.ts` — `createCareEntry` (Admin or Shift Lead) plus `createHealthIssue`/`resolveHealthIssue` (also Admin or Shift Lead — see the Permissions Quick Reference note on this being a judgment call). `[id]/page.tsx` shows open health issues and the last 15 care entries, with a select to tie a new care entry to an already-open issue.
- `src/app/animals/[id]/metrics-actions.ts` — `createWeightEntry` and `createAnimalMetric` (both Admin or Shift Lead — see the Permissions Quick Reference note; this one was an explicit call, not an inferred extension). `[id]/page.tsx`'s "Metrics & Weight" section lists the last 10 `WeightEntry` rows and the last 10 `AnimalMetric` rows (joined with `MetricType` for the name/unit) side by side, each with its own log form. `WeightEntry` and `AnimalMetric` stay two separate lists in the UI, matching why they're two separate tables per `CONTEXT.md` §13 — weight is the timeline of record other things reference, `AnimalMetric` is the generic bucket for Henneke BCS/height/whatever gets added later as a new `MetricType` row.
- **(V2.md Session 1 — replaces the paragraph above in earlier revisions of this file, which described a deleted `pasture-actions.ts`/`fields/page.tsx` pair; fixed as part of the Session 1 cleanup pass, see HANDOFF.md)** `Field`/`PastureAssignment` were generalized into `Location`/`AnimalLocationAssignment` to also cover barn stalls, sick bay, and the covered arena, plus a full append-only move history per animal (day and night tracked independently). `src/lib/locations.ts` — `getLocationHistory(animalId)` and `currentFromHistory(history)` (also `getCurrentLocationAssignments` for the one-shot case), shared by `src/app/animals/[id]/page.tsx`'s "Location" section (day/night current + a `<details>`-gated "View history" expand — never shown by default) and by Vitest coverage, so the derivation logic is exercised for real rather than reimplemented in tests. `src/app/animals/[id]/location-actions.ts` — `createLocationAssignment` (Admin or Shift Lead), always inserts, never updates/closes a prior row. `src/app/locations/actions.ts` + `src/app/locations/page.tsx` — `createLocation` (Admin-only, per-type field validation — e.g. a `FIELD` can't carry a `stallNumber`) and the read-only "plain location list" V1 (`CONTEXT.md` §1/§10's stand-in for the Phase 2 interactive drone-photo map), grouped by which animal(s) currently occupy each location. `Location` is an admin-managed lookup table (same category as `FeedType`/`CareType`) — full CRUD (edit/deactivate an existing `Location`) is deferred to Session 7's Admin Console, matching that session's own stated scope; today only create + list exist.
- `src/app/dashboard/page.tsx` — the cross-entity daily dashboard: one row per `ACTIVE` animal showing today's feeding (baseline + any override), today's medication status (given/missed/pending per regimen), open health issues plus the most recent care entry, latest weight, and current (day) location, all in a single table. Read-only for any signed-in volunteer, same access pattern as `animals/page.tsx` and `locations/page.tsx` — no new Server Actions, it only reads. Queries the last care entry and last weight entry per animal with `orderBy: [{ animalId: "asc" }, { date: "desc" }], distinct: ["animalId"]` (Postgres `DISTINCT ON` under the hood) rather than one query per animal, to keep it to a fixed number of round trips regardless of herd size.
- **(V2.md Session 2 — tier progression & compliance training)** `src/lib/tier.ts` — `computeTiers(volunteer, thresholds, today)`, pure and query-time-only (never a cached field on `Volunteer`, per V2.md's explicit instruction). Returns `computedEligibleTier` (highest tier meeting tenure among tiers that don't require manual release — so this alone never reaches `BLUE`), `actualTier` (promoted to `BLUE` only once `TierThreshold`'s `BLUE` row's tenure is met **and** `blueReleasedAt` is set — otherwise capped at `YELLOW`), and `blueTenureMet` (used to gate release). `Volunteer.firstShiftDate` is set exactly once, from the check-in's own date (not "now", so legacy/backfilled entries stay accurate) — see the guarded `if (!volunteer.firstShiftDate)` block at the end of `src/app/checkin/actions.ts`'s `submitCheckIn`, never touched again after. `src/app/volunteers/tier-actions.ts` — `releaseBlue(volunteerId)`, Admin or Shift Lead. **Blocked (not allowed-with-a-flag) if the volunteer hasn't met Blue's tenure threshold yet** — confirmed directly with James per V2.md's own "genuinely ambiguous, don't assume" flag on this exact question; tenure stays a hard floor an admin can't route around by accident. Also blocked if already released (re-release is rejected outright, doesn't silently reset the original release record). `src/app/tiers/actions.ts` + `src/app/tiers/page.tsx` — `updateTierThreshold` (Admin-only; the four tiers are a fixed set, so this is edit-only, no create/delete) plus a read-only list for everyone else, since exact thresholds are still approximate pending the real written schedule (`CONTEXT.md` §16). `src/app/volunteers/page.tsx` + `src/app/volunteers/[id]/page.tsx` — a volunteer directory (any signed-in volunteer, read-only) and per-volunteer detail showing tenure/tier plus a Blue-release form (Admin/Shift Lead, hidden once already released) and credential/training status with a self-attestation "I read this" button **shown only on your own page** (`currentVolunteer.id === volunteer.id`).
  - **Deviation from V2.md's literal spec, noted per this doc's own instructions:** V2.md describes a new `TrainingRequirement`/`VolunteerTrainingCompletion` pair for compliance training. That would have duplicated the already-existing (but previously unused by any feature) `CredentialType`/`CredentialRecord` models, which `CONTEXT.md` §7 already documents as covering "vaccinations **and** training acknowledgments... under one mechanism" — right down to the seeded `CredentialType` rows (`Rabies Vaccination`, `Volunteer Manual Acknowledgment`, `Blue Handler Class`) already matching this exact domain. Extended `CredentialType` instead: added `isRequired`, `renewalPeriodDays`, `appliesToTier`, `fileUrl`. `src/app/volunteers/training-actions.ts` — `createCredentialType`/`updateCredentialType` (Admin-only) and `logTrainingCompletion` (any signed-in volunteer, **self only** — no admin-on-behalf-of-someone-else path yet, a deliberate MVP scope cut matching V2.md's "self-attestation only for MVP" line). `CredentialRecord.expiresAt` is computed once at write time from `completedDate + CredentialType.renewalPeriodDays` (null `renewalPeriodDays` means one-time, never expires). `src/lib/training.ts` — `getMissingOrExpiredRequiredTraining()`, surfaced on `src/app/training/page.tsx` (Admin/Shift-Lead read) alongside the `CredentialType` admin-edit table.
  - **Also discovered and fixed while here:** `CredentialType.name` had no unique constraint at all, so `db:seed`'s `createMany({ skipDuplicates: true })` had nothing to dedupe against and was silently inserting fresh duplicate rows on every re-run (confirmed 16x duplication in the long-lived local test container). Added `@unique` to `name` (migration `20260719020000_credentialtype_name_unique`, with a defensive dedup step reassigning any `CredentialRecord` FKs before dropping duplicates) and switched seeding to a real `upsert`, matching `Location`'s already-established seeding pattern. **`FeedType`, `CareType`, `WorkType`, and `MetricType` have the same latent gap** (no unique constraint on `name`) — out of scope for this session since nothing here touches them, but worth the same fix before `db:seed` gets re-run against any of them repeatedly.
- **(V2.md Session 3 — volunteer tags & Go Team)** `VolunteerTag` (id, `name` unique, `description`, `minDaysSinceBlueRelease` nullable, `active`) is a generic, extensible tagging lookup table — Go Team is just the first seeded row, the schema doesn't assume it's the only one that will ever exist. `VolunteerTagAssignment` (`volunteerId`, `tagId`, `assignedById`, `assignedAt`, `removedAt`/`removedById` nullable) records who assigned/removed a tag and when; removal is a soft update (`removedAt` set), not a delete — matches `RegularShiftAssignment`'s close-don't-delete pattern rather than `AnimalLocationAssignment`'s pure-append one, since a tag assignment actually gets updated after creation (the removal itself), which is exactly what ChangeLog exists to capture — so unlike `AnimalLocationAssignment`, `VolunteerTagAssignment` **is** in `trackedModels`. `src/app/volunteers/tag-actions.ts`: `createVolunteerTag`/`updateVolunteerTag` are Admin-only (the tag *type*, same lookup-table-definition category as `Location`); `assignTag`/`removeTag` are Admin-or-Shift-Lead (mirrors `Location`'s own type-vs-assignment split). `assignTag` rejects assigning a tag the volunteer already actively holds (app-side check, not a DB constraint, matching this project's general preference for admin-entered lookup data) — but a volunteer can hold multiple different tags at once, and can be reassigned a tag after an earlier assignment was removed (a fresh row, not reviving the old one). `src/lib/tags.ts`'s `getTagEligibilityCandidates(tagId)` powers the "eligible soon" report on `/tags`: any `VolunteerTag` with `minDaysSinceBlueRelease` set gets its own candidates list (Blue-released volunteers who've cleared that many days since `blueReleasedAt`, excluding whoever already actively holds the tag) — read-only for a human to review, never an automatic assignment, per V2.md's explicit instruction. The exact Go Team tenure-since-release number isn't settled (same category of open item as tier tenure timelines — `CONTEXT.md` §16), so it's admin-editable via `/tags` rather than hardcoded; seeded at a placeholder 180 days. `/volunteers/[id]/page.tsx` grew a "Tags" section (list of currently-held tags with a Remove button, plus an assign-tag dropdown scoped to tags not already held) — both gated Admin-or-Shift-Lead, same as the rest of that page's write affordances.
- `.github/workflows/ci.yml` — lint, typecheck, Vitest, and Playwright on every PR/push to `main`. Runs entirely against a throwaway Postgres service container, never Neon — see Testing below.
- `.github/workflows/nightly-backup.yml` — nightly `pg_dump` to R2 plus the Neon keep-alive ping, per `CONTEXT.md` §2.
- `tests/vitest/` — Vitest API/DB tests: `lib/auth.test.ts` and `lib/changelog.test.ts` (the `withChangeLog` extension itself — the single most important thing in this codebase to have real test coverage on, given §4's legal-defensibility requirement), `actions/*.test.ts` (one file per Server Action module, covering role-gating plus DB-write correctness for every action in the app), `routes/*.test.ts` (the photo upload Route Handler and the Clerk webhook handler). `tests/vitest/helpers/` has `db.ts` (real Postgres reset between tests), `factories.ts`, `form.ts`, `auth-mock.ts`, `signals.ts`.
- `tests/e2e/` — Playwright E2E, signed in as real (but disposable, namespaced `e2e-*@volunteer-ops.example.com` — Clerk's live API rejects the RFC-2606 `.test` TLD, see `test-users.ts`) Clerk users via `@clerk/testing`, not a bypass. `fixtures.ts` provides `adminPage`/`shiftLeadPage`/`volunteerPage` (one signed-in Page each) and `openAs(role)` for tests needing two roles active at once. `global-setup.ts` provisions the three test users (find-or-create, idempotent, never torn down) and seeds matching `Volunteer` rows. One spec file per flow area: `checkin`, `animals`, `feeding`, `locations` (V2.md Session 1, replaced `pasture.spec.ts`), `care-and-metrics`, `dashboard`, `tier-and-training` (V2.md Session 2), `volunteer-tags` (V2.md Session 3), plus `smoke.spec.ts` for the homepage/nav (this replaced the old placeholder smoke test rather than sitting alongside it, per this file's own guidance below). Note: `Volunteer` and every admin-managed lookup table (`Location`, `CredentialType`, `TierThreshold`, etc.) are **not** reset between tests within a run (see Testing below) — specs that create rows in those tables need run-unique names to avoid colliding with themselves across repeated local runs, and specs that edit a shared config row (e.g. a `TierThreshold`) must restore it afterward.

## Testing

**Vitest for API/DB tests, Playwright for E2E — both run against a real Postgres, never mocks, never Neon.** Mocking the `withChangeLog` Client Extension would mean not really testing it, and it's the one piece of this codebase with an actual legal-defensibility requirement behind it (§4/§11 of `CONTEXT.md`).

- **Test database:** `docker-compose.test.yml` (a throwaway `postgres:16-alpine`, port 5433, tmpfs-backed) locally; a GitHub Actions `services:` Postgres container in CI on the same port, so `.env.test.example` works unmodified in both places. Never Neon — `tests/vitest/helpers/db.ts` and `tests/e2e/helpers/db.ts` both hard-refuse to run unless `DATABASE_URL` contains `localhost`, specifically to make it impossible for a misconfigured `.env.test` to point a `TRUNCATE` at production data.
- **Commands:** `npm run test:db:reset` (up + migrate + seed) once, then `npm run test:unit` (Vitest) and/or `npm run test:e2e` (Playwright) — or `npm run test:all` for everything. `npm run test:unit`/`test:e2e`/`test:db:migrate`/`test:db:seed` all run via `dotenv-cli` pointed at `.env.test` (copy `.env.test.example` → `.env.test` once locally; it needs no real secrets, just the local Postgres URL).
- **Isolation:** admin-managed lookup tables (`FeedType`, `CareType`, `WorkType`, `MetricType`, `Location`, `CredentialType`, `TierThreshold`, `VolunteerTag`) are seeded once per run and never truncated — nothing in the app writes to them through a `withChangeLog`-tracked path, so there's no isolation benefit to resetting them per-test, only wasted setup (though `Location`/`CredentialType`/`TierThreshold`/`VolunteerTag` **are** writable through plain, untracked admin actions — `createLocation`, `createCredentialType`, `updateTierThreshold`, `createVolunteerTag`, etc. — so tests touching those need run-unique names or an explicit restore, see the `tests/e2e/` bullet above). Every other table is truncated before each Vitest test (`tests/vitest/setup.ts`) and before each Playwright test (`tests/e2e/fixtures.ts`'s auto `resetDb` fixture) — except Playwright also preserves the `Volunteer` table, since the three seeded E2E test volunteers need to survive across tests in the same run.
- **Mocking boundaries — only the true external edges are mocked, never Prisma/business logic:** Vitest mocks `@clerk/nextjs/server`'s `auth()` (per-test via `helpers/auth-mock.ts`, since there's no real Clerk session in a plain Node test), `next/navigation`'s `redirect()`/`notFound()` (they throw outside a real request regardless, so a small typed signal in `helpers/signals.ts` replaces relying on Next's internal, undocumented error shape), `@/lib/r2` (no real R2 bucket needed for Vitest), and `@clerk/nextjs/webhooks`'s `verifyWebhook`. Everything else — Prisma, `withChangeLog`, every Server Action's actual logic — runs for real.
- **Playwright auth is real Clerk, not a bypass:** `@clerk/testing`'s email-ticket sign-in (`clerk.signIn({ page, emailAddress })`) looks the user up via the Clerk Backend API and signs them in with a short-lived ticket — no password, no OTP, and it genuinely exercises `src/proxy.ts` and `src/lib/auth.ts`, unlike injecting a fake session cookie would. This needs a real `CLERK_SECRET_KEY`/`NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` against your actual dev Clerk instance (both locally via `.env` and in CI via repo secrets) — there's no way around that without losing real coverage of the auth path.
- **Coverage:** every Server Action, the photo upload route, the Clerk webhook handler, and the `withChangeLog` extension have Vitest coverage (permission gating, happy-path DB writes, and — for the extension itself — CREATE/UPDATE diffing, tracked-vs-untracked models, `updatedAt` exclusion, append-only behavior). Playwright covers check-in, animal CRUD, feeding baseline+override, location reassignment (day/night independently, append-only history), medication/care/health/metrics logging, the dashboard, tier progression (Blue release + its tenure gate), compliance-training self-attestation, volunteer tag assign/remove + the Go Team eligibility-candidates report, and role-based UI visibility across all three roles — the flows this section used to just say "cover early" are now actually covered.
- **Known gap:** the E2E suite is written and passes `tsc`/lint/Playwright's test discovery, but has not yet been run end-to-end against a live Clerk instance in this session (deferred at James's call, since first execution creates three real — if disposable — user accounts in the live Clerk dev instance). Run `npm run test:e2e` once locally before trusting it fully; see `HANDOFF.md` for what that first run should be expected to do.

## Things to Ask About Before Building, Not Assume

- Anything touching `RecurringCareSchedule` cadence values — real farrier/vet timing isn't documented yet.
- Anything touching `GuestAccess`-style flows — deliberately underbuilt in Phase 1, don't over-invest until it's a confirmed need.
- Exact tier-progression timelines (Green→Orange→Yellow) — currently approximate, get the real written schedule before hardcoding anything UI-facing that states a duration.
- Any UI that surfaces org-wide `ChangeLog` history to `SHIFT_LEAD` — the schema supports it, but the visibility boundary for that role specifically hasn't been finalized.
