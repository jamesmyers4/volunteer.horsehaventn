# HANDOFF.md — Session Log & Continuity Notes

Horse Haven of Tennessee Ops Platform. This document is for picking the project back up cleanly — either you (James) starting a new session, or a future Claude instance getting oriented fast. It's a narrative/status log, not architecture — for *why* the schema looks the way it does, read `CONTEXT.md`. For coding conventions and version-specific traps, read `CLAUDE.md`. This file is "what actually happened and what state things are in."

Last updated: July 20, 2026 (V3.md Session 7 — Admin Console Extensions, **completing the V3 buildout plan**). First build session (schema design through four working feature slices), a same-day follow-up session that added medication and care/health tracking, a second same-day follow-up that added metrics and field/pasture assignment, a third same-day follow-up that added the cross-entity daily dashboard, and a fourth same-day follow-up (via Claude Code, with real push/run access — no more patch-file handoff) that built the full test suite: Vitest for API/DB tests and Playwright for E2E, both against a real throwaway Postgres. A later session (2026-07-18, start of the V2 buildout in `V2.md`) did a dedicated `Horse` → `Animal` rename before touching any V2 feature work, then Location model generalization (V2.md Session 1). The next session cleaned up gaps left in that Session 1 pass, then built out V2.md Session 2 (tier progression + compliance training). The next session (2026-07-19) built V2.md Session 3 (volunteer tags & Go Team). The next session (2026-07-19) built V2.md Session 4 (event scheduler & signup). The next session (2026-07-19) built V2.md Session 5 (shift templates, seasonal hours & kiosk/QR sign-in). The next session (2026-07-19) built V2.md Session 6 (Feed Board & Turnout Board dashboards). The next session (2026-07-19) built V2.md Session 7 (Admin Console) — this completed the V2 buildout plan. A session (2026-07-20, start of the V3 buildout in `V3.md`) built V3.md Session 1 (Intake Groups, Lineage & Adoption Status). **A gap in this log:** V3.md Session 2 (Recurring Facility Tasks) was also built and committed (`prisma/schema.prisma`'s `FacilityTaskType`/`RecurringTaskTemplate`/`FacilityTaskCompletion`, `src/lib/facilityTasks.ts`, `src/app/facility-tasks/`, `tests/vitest/actions/facility-tasks.test.ts`, `tests/e2e/facility-tasks.spec.ts` all exist and pass) but whatever session built it never appended its own dated entry here — flagging rather than reconstructing it after the fact with no firsthand knowledge of that session's own decisions/deviations. The next session (2026-07-20) built V3.md Session 3 (Shift Alerts & Announcements). The next session (2026-07-20) built V3.md Session 4 (Shift Roster & Leader-Driven Attendance). **A second gap in this log, same shape as Session 2's:** V3.md Session 5 (End-of-Shift Checklist skeleton) was also built and committed (`prisma/schema.prisma`'s `ChecklistTemplate`/`ChecklistTemplateItem`/`ShiftReport`/`ShiftReportResponse`, migration `20260720184649_v3_session5_shift_report_checklist`, `tests/vitest/actions/shift-report.test.ts`, `tests/e2e/shift-report.spec.ts` all exist and pass) but whatever session built it never appended its own dated entry here either — flagging rather than reconstructing it after the fact with no firsthand knowledge of that session's own decisions/deviations. The next session (2026-07-20) built V3.md Session 6 (Care Board Parity: Content & Display). The most recent session (2026-07-20) built V3.md Session 7 (Admin Console Extensions) — see the dated section immediately below. **This completes the V3 buildout plan.**

## V3.md Session 7 — Admin Console Extensions (2026-07-20)

**Status: complete — this is the last session in V3.md's own 7-session plan.** No schema changes at all this session — every model this session touches (`FacilityTaskType`, `RecurringTaskTemplate`, `Location`, `ChatChannel`/`ChatMessage`, `IntakeGroup`, `ChecklistTemplate`/`ChecklistTemplateItem`) already existed from Sessions 1–6. Actions, UI, Vitest, and Playwright all built and green (341/341 Vitest, 100/101 Playwright, confirmed on the existing local Docker test DB rather than a from-scratch reset since no migration was needed this session — the one Playwright failure is the pre-existing, already-documented `shift-roster.spec.ts` time-of-day flake from Session 6's own HANDOFF entry below, re-confirmed as unrelated: this run happened at 5:25 PM local time, squarely in the PM-shift window that flake requires, and it fails identically with or without this session's changes).

- **Same deviation pattern V2.md Session 7 already established, per this doc's own "note the deviation" instruction:** V3.md's scope list asks for "CRUD screens for: `IntakeGroup`, `FacilityTaskType`, `ChecklistTemplate`/`ChecklistTemplateItem`." Two of those three (`IntakeGroup` on `/intake-groups`, full create/edit/deactivate from Session 1; `ChecklistTemplate`/`ChecklistTemplateItem` on `/checklists`, full CRUD from Session 5) already had working Admin-gated CRUD built in their own prior sessions — rebuilding either under `/admin` would just be duplicate parallel UI. Only `/admin/page.tsx` gained links out to both (previously missing from that console home entirely — a real gap, not a deliberate prior omission). `FacilityTaskType` was the one genuinely missing screen, so it's the only new CRUD page this session builds under `/admin` directly.
- **A further, flagged deviation from this model's own schema comment:** `FacilityTaskType.category` is `@unique` against the fixed three-value `FacilityTaskCategory` enum — the schema comment right on that enum already draws the parallel to `ShiftTemplate.shiftType`'s own capped-table shape, but the `FacilityTaskType` model's own comment separately says "full CRUD deferred to the Admin Console," which reads more like the genuinely-growable `WorkType`/`FeedType`/`CareType` lookup tables. Since `prisma/seed.ts` already upserts all three categories in every environment, a create form could never succeed in real use (the unique constraint always fires) — confirmed directly: `FacilityTaskType` is a lookup table never truncated between Vitest runs (`tests/vitest/helpers/db.ts`), so by the time any test runs, all three category rows already exist with no hard-delete path to free one up. Built edit-only (name, active) instead, `src/app/admin/facility-task-types/`, matching the `ShiftTemplate`/`TierThreshold` precedent (`src/app/settings/actions.ts`'s `updateShiftTemplate`) rather than the model comment's looser wording.
- **`src/lib/facilityTasks.ts`** gained `getRecurringTasksForMonth(monthStart)` (same derive-don't-cache principle as the existing `getExpectedFacilityTasks`, just expanded across every date in a month instead of one day — matches `RecurringTaskTemplate` rows against each date's own `dayOfWeek`, deliberately with no `shiftType` filter since the calendar shows both AM and PM slots per day at once), plus `parseMonthParam`/`monthParamFor` for `?month=YYYY-MM` navigation (a malformed/missing param falls back to the current UTC month rather than throwing, since a bad query param shouldn't 500 the page).
- **`src/app/admin/facility-tasks/page.tsx`** — the monthly calendar view V3.md's scope explicitly asks for: a 7-column/weeks-grid table (Sunday-start, matching `getUTCDay()`'s own convention already used throughout this codebase) with each cell showing that date's expanded AM/PM slots, month-to-month navigation via plain links (`← July` / `August →`, zero client JS per this codebase's established convention), and the same "assign a task type + location + weekday + shift to a recurring slot" management form V3.md's own wording describes — reusing `createRecurringTaskTemplate`/`updateRecurringTaskTemplate` from `src/app/facility-tasks/actions.ts` unchanged in shape, not duplicated. Both actions gained one new optional `redirectTo` hidden form field (defaulting to their original `/facility-tasks` target when absent, so `/facility-tasks`'s own existing forms and every existing Vitest assertion needed zero changes) — same precedent Session 6-of-V2 already set for `createFeedingOverride`/`createLocationAssignment` — so the Admin Console's own calendar form lands back on itself (with the month preserved) instead of navigating away to the plain list.
- **The ad hoc quick-add path for `FacilityTaskCompletion` needed no new work** — it already lives on the plain `/facility-tasks` page from Session 2, genuinely separate from this new calendar (V3.md's own "intentionally outside the calendar" wording), and already has Vitest/Playwright coverage from that session. This calendar page links out to it rather than duplicating it.
- **`Location.requiresStripClean` toggle also needed no new work** — confirmed already exposed on `/admin/locations` (`src/app/admin/locations/page.tsx`) with existing Playwright coverage (`facility-tasks.spec.ts`'s "an Admin toggles requiresStripClean..." test), predating this session. V3.md's scope line describing this as work to do here was already satisfied.
- **`src/app/admin/alerts/page.tsx`** — the pinned-message composer V3.md's scope asks for: a channel selector scoped to `BROADCAST` + both `SHIFT` channels only (the `ADMIN` channel is excluded from the choices since `src/lib/alerts.ts`'s `isAlertVisibleForViewer` never banners it anyway), severity, and an optional expiry — posting through the **existing, unmodified** `postChatMessage` action from `src/app/chat/actions.ts` (a hidden `pinned=on` field) rather than a new action or a parallel Alert table, per V3.md's explicit "no separate Alert table involved." Also lists currently-live pinned alerts (via the existing `isLiveAlert` from `src/lib/alerts.ts`) for at-a-glance confirmation. Submitting redirects to the target channel's own `/chat` view (that's `postChatMessage`'s existing, unchanged redirect target) rather than back to `/admin/alerts` — the message really posted there, so landing on it is the natural confirmation; no `redirectTo` override was added to `postChatMessage` for this.
- **All four new/extended `/admin/*` routes (`/admin/facility-task-types`, `/admin/facility-tasks`, `/admin/alerts`, plus the console home's new links) use the same real `requireRole(["ADMIN"])` try/catch pattern as every other Admin Console page** — a Shift Lead is blocked with "Not authorized" and zero admin data fetched, not just a hidden nav item, matching V3.md's own explicit test-coverage ask. Note this is narrower than the underlying actions themselves: `createRecurringTaskTemplate`/`updateRecurringTaskTemplate` remain Admin-or-Shift-Lead at the action layer (unchanged from Session 2, since Shift Leads still manage day-to-day templates from the plain `/facility-tasks` page) — only the polished Admin Console *calendar view* of that same data is Admin-only, matching how `/admin/locations` is Admin-only even though `createLocation` itself always was too.
- **Test coverage:** Vitest — `tests/vitest/lib/facilityTasksCalendar.test.ts` (`parseMonthParam`/`monthParamFor` pure-function cases, `getRecurringTasksForMonth`'s weekday expansion against a full month — including the "returns both AM and PM for the same day" case the single-shift daily view can't express — and deactivated-template exclusion), `tests/vitest/actions/admin-facility-task-types.test.ts` (`updateFacilityTaskType` permission gating + correctness, with an `afterEach` restore of the shared seeded "Trough Clean" row — same lookup-table-never-truncated discipline `settings.test.ts` already established for `ShiftTemplate`/`FarmSettings`). `tests/e2e/admin.spec.ts` extended: the existing Shift-Lead route-block loop now also covers the three new `/admin/*` paths, the console-home link-inventory test checks all four new links, plus four new dedicated tests — renaming/deactivating a `FacilityTaskType` (with an in-test restore back to its original name/active state, matching the Vitest-side discipline), assigning a new recurring slot from the calendar and confirming it lands on the correct weekday cell (using a fixed 2026-07-01-is-a-Wednesday reference date, same deterministic-reference-date pattern `facility-tasks.spec.ts` already uses), month-navigation links moving the calendar forward/back a month, and an Admin composing a pinned alert targeting a specific `SHIFT` channel with severity + an expiry from `/admin/alerts` (confirmed via a direct Prisma read, not just the UI).
- **Not applied to live Neon** — no migration this session at all (no schema changes), so nothing new is queued for the eventual cutover beyond what Sessions 1–6 already have pending.
- **Per `V3.md`'s own rule, and because this was explicitly the last session in its plan: stopping here. There is no Session 8 — the V3 buildout described in `V3.md` is complete,** same milestone shape as V2.md Session 7's own closing note. What comes after (live Neon cutover, Vercel deployment, the Phase 2 items `CONTEXT.md` §15 lists, or a future V4) is outside `V3.md`'s scope and needs its own explicit go-ahead from James.

## V3.md Session 6 — Care Board Parity: Content & Display, Not Schema (2026-07-20)

**Status: complete.** No schema change and no migration this session, exactly as V3.md's own framing predicted ("mostly a content/seeding and display logic pass"). Seed data, Feed Board display, and Playwright coverage all built and green (332/332 Vitest — unchanged, since nothing here added a new Server Action — and the full Playwright suite passes except one pre-existing, out-of-scope failure flagged below). Per V3.md's own instructions, this session stops here — Session 7 (Admin Console Extensions) is not started.

- **Audit findings, confirmed against the real schema before writing anything:** `FeedingBaseline.shift` already splits Grain/Hay by AM/PM, `MedicationRegimen.startDate`/`endDate` already supports time-boxed meds, and the Feed Board's/animal page's existing `OR: [{ endDate: null }, { endDate: { gte: today } }]` query already correctly excludes an expired regimen from "current" — all three needed no code change, exactly as V3.md's own findings predicted.
- **Confirmed with James before implementing, per V3.md's own explicit "confirm this split with the user" flag:** the real board's short-lived, date-specific "ATTN" handling flags (e.g. "cold hose if breaths per min exceed 20") are modeled as `CareEntry` rows against a new `CareType`, **not** a new field on `Animal` — with one adjustment to V3.md's own suggestion: category `MEDICAL` (a health-driven watch instruction), not `OTHER`, since these are genuinely medical in nature, not a catch-all.
- **`prisma/seed.ts`:** three new `CareType` rows — "Topical Spray" (`SEASONAL`) and "Leg Wrap" (`GROOMING`, a judgment call — a protective wrap during routine handling, mirroring the existing "Grooming"/"Fly Mask / Spray" split) close the real board's "Skin Care" gap alongside the already-existing "Fly Mask / Spray"; "ATTN / Handling Flag" (`MEDICAL`) is the new CareType the ATTN-flag decision above hangs off. No code change needed to log against it — `createCareEntry` (`src/app/animals/[id]/care-actions.ts`) and its form on the animal detail page are already fully generic over `CareType`, so this is purely a seed-data addition.
- **`src/app/feed-board/page.tsx`** gained two columns, closing V3.md's "four distinct, correctly-attributed sources" requirement: **Skin Care** (today's `CareEntry` rows whose `CareType.category` is `SEASONAL`/`GROOMING`, one query shared with the column below rather than two separate DB round trips) and **Handling Notes** (the standing `Animal.handlingNotes` value plus, styled distinctly in red, any today-dated `CareEntry` matching the "ATTN / Handling Flag" `CareType` by name — two genuinely different sources sharing one column since together they answer "what to know about handling this horse today"). Matches this page's existing "one query, filter in memory" convention (already used for `mainFeeds`/`hayFeeds`) rather than adding new lib abstraction for a page that was never Vitest-unit-tested in the first place (same precedent Session 6-of-V2 already set: board pages get Playwright coverage, not a dedicated lib+Vitest layer).
- **Dashboard (`src/app/dashboard/page.tsx`) was deliberately left untouched** — V3.md's gap analysis and test-coverage list both name "the Feed Board query" specifically (the direct analog of the real Horse Care Board this session closes parity against), not the cross-entity dashboard; widening scope to a second page wasn't asked for.
- **A real, pre-existing, unrelated bug found while running the full Playwright suite, not introduced by this session:** `tests/e2e/shift-roster.spec.ts`'s first test (V3.md Session 4) fails consistently late in the day — reproduced identically with this session's changes fully stashed out, confirming it predates this session. Root cause, not yet fixed (flagging per this project's established practice rather than silently absorbing an out-of-scope fix): the test hardcodes `shiftType: "AM"` for both the `RegularShiftAssignment` and the roster page it visits, but the walk-on's real kiosk check-in goes through `determineShiftTypeForNow()` (`src/lib/shifts.ts`), which picks whichever shift window is closer to the *actual* current time with no test-provided override — running the suite in the Tennessee evening (confirmed: local run at ~4:39 PM CDT / 20:39 UTC, well past the 16:00-19:00 PM window but far from the next 09:00 AM one) resolves to `PM`, so the walk-on's `CheckIn` lands on a `Shift` occurrence the AM-scoped roster page never queries — the row the test expects (`"Already checked in (KIOSK)"`) never renders. This is a test-authoring gap (no way to pin "now" for the kiosk step), not an application bug; worth a fix in some future session (e.g. accepting an injectable `now` the way `determineShiftTypeForNow` already does, or restructuring the test to assert against whichever shift the kiosk actually resolved to) but out of Session 6's own scope to fix here.
- **Test coverage:** `tests/e2e/boards.spec.ts` gained two tests — one confirming Skin Care and Handling Notes render as sources distinct from Feed/Meds for the same animal/day (the exact CareEntry-vs-MedicationRegimen distinctness V3.md's test-coverage list asks for, plus the standing-handlingNotes-vs-dated-ATTN-flag split), and one confirming a `MedicationRegimen` with a past `endDate` is excluded from the board while its `MedicationLog` history remains directly queryable via Prisma (asserted with a direct `prisma.medicationLog.findMany` read in the same test, matching this suite's established precedent of raw-DB assertions alongside UI ones). Full suite run (`docker compose down -v` reset, `npm run test:unit`, `npm run test:e2e`): 332/332 Vitest, 93/94 Playwright (the one failure is the pre-existing `shift-roster.spec.ts` issue described above, confirmed unrelated by re-running against a clean stash).
- **Not applied to live Neon** — no schema change this session, so nothing new is queued beyond what Sessions 1-5 already have pending.
- **Per `V3.md`'s own rule, stopping here — do not start Session 7 (Admin Console Extensions) until James starts a new session for it.**

## V3.md Session 4 — Shift Roster & Leader-Driven Attendance (2026-07-20)

**Status: complete.** Schema, migration, lib logic, actions, UI, Vitest, and Playwright all built and green (319/319 Vitest, 90/90 Playwright, confirmed on a from-scratch `docker compose down -v` reset, not just an incremental run). Per V3.md's own instructions, this session stops here — Session 5 (End-of-Shift Checklist) is not started.

- **Schema:** `Shift.assignedLeadId` (nullable FK to `Volunteer`, reverse relation `Volunteer.ledShifts`) — occurrence-scoped shift leadership, layered on top of the existing global `SHIFT_LEAD` role rather than replacing it, exactly as V3.md specifies. No new tables — attendance continues to be plain `CheckIn` rows, using the `checkInMethod: ADMIN_ENTRY` value that was already sitting unused in the enum. Migration `20260720161338_v3_session4_shift_assigned_lead`, auto-generated via `prisma migrate dev` against the local Docker test DB only, confirmed purely additive (one nullable column + index + FK, no drops/renames) — **not yet applied to live Neon**, same deferred-until-confirmed pattern as every migration since the `Horse`→`Animal` rename (now sixteen migrations deep queued for the coordinated cutover). `Shift` was already in `trackedModels` (V2.md Session 5), so the new field's writes are ChangeLog-captured automatically, no extra wiring needed.
- **`src/lib/shiftRoster.ts`** (pure-logic-plus-DB pattern, same as `src/lib/facilityTasks.ts`): `getDefaultRoster(date, shiftType)` derives the expected list at render time, never pre-generated — every `Volunteer` with a matching active `RegularShiftAssignment` (`active: true`, `date` within `startDate`/`endDate`) for that date's `dayOfWeek` + the given `shiftType`, unioned with anyone who already has a `CheckIn` row for that `Shift` (the walk-on/self-check-in case V3.md explicitly calls out) — a volunteer present in both sources merges into one entry rather than appearing twice. `canManageShiftRoster(actor, shift)` is the shared permission predicate (`actor.role` is `ADMIN`/`SHIFT_LEAD`, OR `actor.id === shift.assignedLeadId`), used by both the Server Action and the page component so the UI's "can you see the submit form" check can never drift from the action's own enforcement. Re-exports `startOfDay`/`dayOfWeekFor` from `src/lib/facilityTasks.ts` rather than re-deriving the same UTC-date-string logic a third time.
- **`src/app/checkin/roster/actions.ts`**: `assignShiftLead` (Admin-or-Shift-Lead, mirrors `setShiftActualTimes`'s existing permission split on this same `Shift` model — a global lead naming who's actually running one occurrence, which may be a plain `VOLUNTEER` filling in) and `submitRosterAttendance` (gated by `canManageShiftRoster`). Bulk submit creates a `CheckIn` (`checkInMethod: ADMIN_ENTRY`, `loggedById` = the submitting leader) **only** for a `presentVolunteerIds` entry that doesn't already have one for this `Shift` — an existing row (real QR/kiosk self-check-in) is never read for update, only checked for existence, so its actual method/times are always preserved exactly as V3.md requires. `presentVolunteerIds` deliberately carries both the default-roster checkboxes and the walk-on multi-select under one form field name — by submit time there's no meaningful difference between "on the standing roster" and "added as a fill-in," both just become present. Time resolution: a leader-entered `checkInTime`/`checkOutTime` wins if provided, else falls back to `resolveShiftTimesForOccurrence()` (template + `FarmSettings.activeSeason` + any `Shift.actualStartTime`/`actualEndTime` override) — reusing `src/lib/shifts.ts` rather than re-deriving it. `maybeSetFirstShiftDate()` runs per newly-created `CheckIn` so a walk-on's tenure clock (V2.md Session 2) starts correctly even when logged by someone else, not just on self check-in.
- **The one open question V3.md explicitly flagged ("confirm whether a volunteer can already edit their own CheckIn row's times") — confirmed no such path existed, so this session added it.** `updateOwnCheckIn` in `src/app/checkin/actions.ts`: self-only (rejects if `existing.volunteerId !== volunteer.id`, matching this codebase's established self-attestation pattern — `CredentialRecord`, `EventSignup` — over an admin-on-behalf-of-someone-else path), works on any of the volunteer's own `CheckIn` rows, not just `ADMIN_ENTRY` ones. `/checkin` gained a "Your recent check-ins" section (last 5, newest first) with an inline per-row edit form (time in/out/notes), so a volunteer can correct a bulk `ADMIN_ENTRY` default the same day it's logged.
- **New page `src/app/checkin/roster/page.tsx`** (`/checkin/roster?date=...&shiftType=...`, defaults to today/AM): read-only for anyone signed in (org-wide shift visibility already established for Shift Lead, and a plain Volunteer can at least see who's expected); the bulk-submit form (checkbox-per-roster-entry, default checked = "everyone present, uncheck absentees" per V3.md, plus a walk-on multi-select and shared time-in/out fields) only renders when `canManageShiftRoster` says yes. A small "Shift lead for this occurrence" panel lets Admin/Shift-Lead set or clear `assignedLeadId` via a plain `<select>`. Linked from `/checkin` ("Shift roster") and the homepage nav.
- **A real design call, not explicitly dictated by V3.md:** the bulk-submit form uses one shared check-in/check-out time pair for the whole batch (defaulted from the resolved shift time, directly editable) rather than a per-volunteer time field — V3.md's wording ("checkInAt/checkOutAt defaulted... unless the leader enters different values directly") reads consistently either way, and a shared pair matches how the existing `/checkin` and `setShiftActualTimes` forms already work in this codebase. A volunteer whose actual time differs from the batch can always self-correct via the new `updateOwnCheckIn` path afterward.
- **Test coverage:** Vitest — `tests/vitest/actions/roster.test.ts` (22 tests): `getDefaultRoster`'s dayOfWeek/shiftType/active/date-range matching and exclusion cases, the regular-assignment-plus-existing-checkIn merge case, `canManageShiftRoster`'s three branches as a pure function, `assignShiftLead` permission gating + clearing back to null, `submitRosterAttendance` permission gating (including the plain-Volunteer-as-named-lead allow case), the no-duplicate/no-overwrite guarantee against a real pre-existing `QR` check-in, walk-on addition, all three time-resolution paths (template+season default, Shift-level override, direct leader entry), and `updateOwnCheckIn`'s self-only enforcement. One timezone-fragile assertion pattern caught and fixed before it caused flaky CI runs elsewhere: comparing `checkInAt.toISOString().slice(11, 16)` against a literal "09:00" assumes the process runs in UTC, which isn't guaranteed — switched to comparing full `Date` `.getTime()` values against the same `new Date("...T09:00:00")` construction the action itself uses, matching the existing convention already established in `tests/vitest/actions/checkin.test.ts`. `tests/e2e/shift-roster.spec.ts` (4 tests): default-roster derivation + bulk submit leaving a real kiosk self-check-in completely untouched, walk-on addition via the multi-select, the full permission-gating story (plain Volunteer sees read-only, gains submit access once named `assignedLeadId`, and their `loggedById` on the resulting `CheckIn` is correctly their own id), and a volunteer self-editing an `ADMIN_ENTRY` row's notes/time from `/checkin`. Reused the `openAs()` two-actor pattern (not `adminPage`+`volunteerPage` together in one test) per the already-documented Clerk "already signed in" gotcha from prior sessions.
- **Not applied to live Neon** — same holding pattern as every migration since the `Horse`→`Animal` rename.
- **Per `V3.md`'s own rule, stopping here — do not start Session 5 (End-of-Shift Checklist) until James starts a new session for it.**

## V3.md Session 3 — Shift Alerts & Announcements (2026-07-20)

**Status: complete.** Schema, migration, lib logic, actions, UI, Vitest, and Playwright all built and green (297/297 Vitest, 86/86 Playwright, confirmed on a from-scratch `docker compose down -v` reset, not just an incremental run). Per V3.md's own instructions, this session stops here — Session 4 (Shift Roster & Leader-Driven Attendance) is not started.

- **Schema:** new `AlertSeverity` enum (`INFO`/`WARNING`/`URGENT`), `ChatChannel.shiftType` (nullable `ShiftType`, set only when `type: SHIFT`, mirroring `ShiftTemplate.shiftType`), `ChatMessage.severity`/`expiresAt`/`pinned` (all nullable/defaulted so ordinary chat messages are untouched). Migration `20260720143257_v3_session3_shift_alerts_announcements`, purely additive, auto-generated and applied against the local Docker test DB only — **not yet applied to live Neon**, same deferred-until-confirmed pattern as every schema change since the `Horse`→`Animal` rename (now fifteen migrations deep queued for the coordinated cutover). `ChatMessage`/`ChatChannel` are **not** added to `trackedModels` — same no-legal-defensibility-angle reasoning as `Location`/`Event`, nothing in V3.md asks for it.
- **No prior chat feature existed at all before this session** — `ChatChannel`/`ChatMessage` were schema-only since the original Phase 1 build (CONTEXT.md §14 described the intent, nothing implemented it). `src/app/chat/actions.ts`'s `postChatMessage` (open to any signed-in volunteer for ordinary posting; `pinned: true` rejected server-side for anyone but ADMIN, not just hidden in the UI) and a minimal `src/app/chat/page.tsx` (channel nav, message history, post form with pin/severity/expiresAt fields shown only for ADMIN) are genuinely new. A polished admin composer with channel targeting is explicitly V3.md Session 7's own scope ("A pinned-message composer for ADMIN... no separate Alert table involved") — this session's `/chat` page is the same "basic functional page now, polished Admin Console screen later" split `IntakeGroup`/`Location` already established, not a placeholder.
- **`src/lib/alerts.ts`** (pure-logic-plus-DB pattern, same as `src/lib/facilityTasks.ts`/`src/lib/shifts.ts`): `isLiveAlert(message, now)` (pinned AND `now` between `createdAt`/`expiresAt` inclusive, null `expiresAt` never expires), `isAlertVisibleForViewer(channel, viewerShiftType)` (BROADCAST always visible, SHIFT only to a matching-shiftType viewer, ADMIN never banners), `getViewerShiftType(volunteerId, now)` (prefers the volunteer's currently-open `CheckIn`'s shift — the "on" case — falling back to `determineShiftTypeForNow()` for "viewing" outside an active check-in, reusing `src/lib/shifts.ts`/`src/lib/farmSettings.ts` rather than reimplementing), `getLiveAlerts(volunteerId, now)` (fetches all pinned messages, filters in memory with the same two pure functions unit-tested directly — same "share real logic between UI and tests" precedent as `getExpectedFacilityTasks`).
- **Global banner (`src/app/AlertBanner.tsx`), wired into `src/app/layout.tsx`:** an async Server Component rendered before `{children}` in the root layout, so it's unavoidable on every authenticated page rather than something each page has to remember to include. Renders nothing for a signed-out visitor or once there are no live alerts. Wrapped in `role="region" aria-label="Live alerts"` (a real accessibility landmark, not a test-only hook) so Playwright — and screen readers — can address it as a distinct region from a page's own chat history list, which can legitimately show the same message text at the same time.
- **A real, previously-unencountered Next.js 16 gotcha, caught by actually running Playwright against a production build, not assumed:** the first version of `postChatMessage` relied on `redirect()` alone (this codebase's universal pattern — every prior Server Action redirects to a page whose *own* content reflects the write, and that page segment does refresh correctly). This is the **first** action whose write needs to invalidate something rendered in an ancestor layout segment (`AlertBanner`, shared across the whole app) rather than just its own destination page — and `redirect()`'s implicit refresh did not reliably extend to that ancestor segment: an Admin pinning a message from `/chat` saw the new message correctly in `/chat`'s own history list (proving the write and the page-level render were both fresh) but the global banner stayed empty until some other trigger caused a fresh layout render. Fixed with `revalidatePath("/", "layout")` (Next's documented mechanism for invalidating every route sharing a layout) called right before `redirect()` in `postChatMessage`. This is a narrow, targeted addition — not a new house convention every action needs, just the first one whose blast radius genuinely extends above its own page. **Second-order fix required:** `revalidatePath()` throws ("static generation store missing") when a Server Action is invoked directly from Vitest, outside a real Next.js request — added a `vi.mock("next/cache", () => ({ revalidatePath: vi.fn() }))` to `tests/vitest/setup.ts`, same "stub the Next.js runtime boundary, not business logic" category as the existing `next/navigation` mock for `redirect()`/`notFound()`.
- **A real, pre-existing, unrelated bug found and fixed while verifying this session's own Playwright run against the full suite:** `tests/e2e/facility-tasks.spec.ts` (V3 Session 2) intermittently failed with a `completedById` mismatch — root-caused to `admin.spec.ts`'s "an Admin changes a volunteer's role from Volunteer to Shift Lead" test (V2 Session 7), which creates a throwaway `Volunteer` via `createThrowawayVolunteer` and promotes it to `SHIFT_LEAD` but never reverts it (correctly so — no hard deletes on `Volunteer`, and it isn't truncated between E2E tests within a run). Once that test runs earlier in the same suite (alphabetically, `admin.spec.ts` always precedes `facility-tasks.spec.ts`), two `SHIFT_LEAD`-role volunteers exist, and `facility-tasks.spec.ts`'s `prisma.volunteer.findFirstOrThrow({ where: { role: "SHIFT_LEAD" } })` (no `orderBy`) can non-deterministically resolve to either one — confirmed directly by querying the DB after a run (`Volunteer` rows: "E2E Shift Lead" and a leftover "Throwaway ..." both with `role: SHIFT_LEAD`). This reproduced even on a from-scratch `docker compose down -v` reset (it's a same-run ordering interaction between two existing spec files, not accumulated debris across separate runs). **Confirmed with James before fixing**, since it's outside this session's own V3.md scope — fixed `facility-tasks.spec.ts`'s two ambiguous `role`-only lookups (`SHIFT_LEAD` and `VOLUNTEER`) to filter by the canonical seeded `TEST_USERS` email instead; this session's own `alerts.spec.ts` had the identical latent bug in one spot (`role: "VOLUNTEER"`) and got the same fix pre-emptively. `role: "ADMIN"` lookups elsewhere in the suite are unaffected — no test anywhere promotes/creates a second `ADMIN`-role volunteer, so that specific pattern stays safe.
- **Test coverage:** Vitest — `tests/vitest/lib/alerts.test.ts` (`isLiveAlert` boundary conditions at both `createdAt`/`expiresAt`, null-`expiresAt` standing alerts, `isAlertVisibleForViewer` for all three channel types, `getViewerShiftType`'s open-CheckIn-vs-time-of-day-fallback branches, `getLiveAlerts` end-to-end scenarios matching V3.md's own worked cases), `tests/vitest/actions/chat.test.ts` (`postChatMessage` auth/pin-permission gating — including a Shift Lead explicitly rejected, not just a plain Volunteer — and severity/expiresAt forced null on any unpinned message regardless of role or form contents). `tests/e2e/alerts.spec.ts` (5 tests): pin-controls visibility across all three roles, a BROADCAST alert pinned by an Admin banners on a completely different page for a completely different role, SHIFT-channel scoping to a volunteer's own checked-in shift (with a wrong-shift alert confirmed absent), an expired standing alert excluded from the banner but still present in normal chat history, and an unpinned message never bannering anywhere while remaining visible in history.
- **Not applied to live Neon** — same holding pattern as every migration since the `Horse`→`Animal` rename.
- **Per `V3.md`'s own rule, stopping here — do not start Session 4 (Shift Roster & Leader-Driven Attendance) until James starts a new session for it.**

## V3.md Session 1 — Intake Groups, Lineage & Adoption Status (2026-07-20)

**Status: complete.** Schema, migration, actions, UI, Vitest, and Playwright all built and green (257/257 Vitest, 77/77 Playwright, confirmed on a from-scratch `docker compose down -v` reset, not just an incremental run). Per V3.md's own instructions, this session stops here — Session 2 (Recurring Facility Tasks) is not started.

- **Confirmed with James before building, per V3.md's own "confirm with the user if that seems like overkill" flag:** `AnimalRelationship` is ChangeLog-tracked (added to `trackedModels` in `src/lib/prisma.ts`) — a lineage claim on a `legalCase` animal sits on the same legal-defensibility surface as `Animal` itself (CONTEXT.md §4/§11).
- **Schema:** new `RelationType` enum (`SIRE_OF`/`DAM_OF`/`SIBLING_OF`/`OTHER`), `IntakeGroup` (lookup-ish, not tracked — same category as `EventCategory`), `Animal.intakeGroupId` (nullable), `AnimalRelationship` (one row per real relationship, two named relations to `Animal` — `AnimalRelationshipFrom`/`AnimalRelationshipTo` — plus a real `recordedBy` Volunteer FK, matching the `AnimalLocationAssignment`/`EventSignup` FK convention rather than the older plain-string `performedBy` pattern), `AnimalStatus` gained `FOSTER`/`PENDING_ADOPTION`, `Placement.placementGroupId` (nullable shared token, no new `PlacementGroup` table). Migration `20260720114911_v3_session1_intake_groups_lineage_placement_group`, auto-generated via `prisma migrate dev` and confirmed purely additive (new enum/table/columns only, no drops/renames) — safe to auto-generate this time, unlike the `Horse`→`Animal` rename. **Not yet applied to live Neon**, same deferred-until-confirmed pattern as every schema change since the rename (now twelve migrations deep queued for the coordinated cutover).
- **Inverse relationship display, never a second stored row:** `src/lib/relationships.ts`'s `getRelationshipsForAnimal(animalId)` queries both directions (`animalId` and `relatedAnimalId`) and derives the label at read time (`SIRE_OF` reads "Sire of X" from the sire's own page, "Sire" from the offspring's page; `SIBLING_OF` reads the same both ways) — matches V3.md's explicit "do not also store the inverse row" instruction. Verified against the exact worked scenario V3.md gives: Guinness `SIRE_OF` both Rowan and Shannon, with Bridgette `DAM_OF` Rowan and Sorcha `DAM_OF` Shannon — confirming one animal can carry an independent sire *and* dam relationship at once (`tests/vitest/lib/relationships.test.ts`).
- **HHT Days ("days at Horse Haven") computed at query time, never stored** — `src/lib/placements.ts`'s `computeHhtDays()`, same "computed-not-cached" convention as `src/lib/tier.ts`. Uses the animal's latest *still-active* (`returnedDate` null) `Placement.placedDate` if one exists, else counts to today — so a `RETURNED` animal's HHT Days correctly resumes counting from intake rather than freezing at a placement date that no longer applies. Surfaced on the animal detail page next to Intake Date; no separate "Adoption Status Board" page was built this session (Foster/Pending Adoption/Adopted-in-last-90-days board is background motivation in V3.md's source-data notes, not an explicit Data Model/Business Rules/Test Coverage requirement of Session 1 itself — flagging in case that board turns out to be wanted as its own slice).
- **The real design decision this session made, not explicitly dictated by V3.md:** "moving `Animal.status` to `ADOPTED` should coincide with a `Placement` row being created/finalized" — checked first (per V3.md's own instruction) whether `updateAnimal` already enforced or assumed this; it didn't, it just set the field directly. Fixed by making `createPlacement` (`src/app/animals/[id]/placement-actions.ts`, Admin-only, matching `Animal` core-status write access) the **sole path** that can set `ADOPTED`: it creates one `Placement` row per animal (adopter info) and flips that animal's status in the same action, for one animal or several sharing a freshly generated `placementGroupId` (co-adoption — a bonded pair or trio kept together, per the source-data notes). `updateAnimal`/`createAnimal` (`src/app/animals/actions.ts`) now reject any direct transition *into* `ADOPTED`, throwing an error directing the caller to Record Placement instead — but editing an already-`ADOPTED` animal's other fields (status left unchanged) is unaffected, so this doesn't retroactively break records that predate this session. No `$transaction` wrapping the per-animal loop in `createPlacement` — no existing precedent for one in this codebase, and a partial write (e.g. the process dying mid-loop) just leaves some animals correctly placed and others not-yet-placed, a normal "keep going" state that doesn't need all-or-nothing atomicity.
- **`IntakeGroup` assignment is Admin-or-Shift-Lead** (`src/app/animals/[id]/intake-group-actions.ts`'s `assignIntakeGroup`) — matches the `AnimalLocationAssignment`/`VolunteerTagAssignment` "assignment vs. type-definition" permission split explicitly named in V3.md, **not** the Admin-only `updateAnimal` core-fields gate, even though `intakeGroupId` lives directly on `Animal`. Since `Animal` is a tracked model, this still routes through `withChangeLog` like every other `Animal` write. The `IntakeGroup` *type* itself (`src/app/intake-groups/actions.ts`: `createIntakeGroup`/`updateIntakeGroup`, plus `/intake-groups` list + `/intake-groups/[id]` detail pages) is Admin-only, same lookup-table-config category as `Location`/`EventCategory` — full create+update(+`isActive` deactivate) CRUD built now rather than deferred to Session 7's Admin Console, since V3.md's own Session 1 test-coverage list explicitly asks for "IntakeGroup CRUD," unlike the narrower create-only precedent `Location` set in V2 Session 1. `AnimalRelationship` write access (`src/app/animals/[id]/relationship-actions.ts`'s `createAnimalRelationship`) is also Admin-or-Shift-Lead, per V3.md's explicit instruction — create-only (no update), since the inverse-derivation design means there's only ever one row to correct and V3.md's own test-coverage list doesn't ask for an edit path.
- **Animal detail page** (`src/app/animals/[id]/page.tsx`) gained an "Intake Group & Family" section (group link + assign form, relationships list + add form) and a "Placement / Adoption" section (current placement + co-adopted links if any, or a Record Placement form gated to Admin and hidden once the animal is already `ADOPTED`) — both following the same read-for-everyone/write-form-gated-by-role pattern as every other section on this page. `AnimalFormFields.tsx` gained Foster/Pending Adoption as selectable statuses; `ADOPTED` stays in the list too (so an already-adopted animal's edit page still renders correctly) but is now effectively unreachable from this form going forward. `/animals` list's status color-badge map extended for the two new statuses.
- **Test coverage:** Vitest — `tests/vitest/lib/placements.test.ts` (pure `computeHhtDays`, no DB), `tests/vitest/lib/relationships.test.ts` (the Guinness/Rowan/Shannon/Bridgette/Sorcha scenario plus the empty-list and `SIBLING_OF`-symmetric cases), `tests/vitest/actions/intake-groups.test.ts` (`IntakeGroup` CRUD permission gating + `isActive` deactivation + `assignIntakeGroup`'s Admin-or-Shift-Lead split + `Animal.intakeGroupId` staying nullable/unenforced), `tests/vitest/actions/animal-relationships.test.ts` (permission gating, ChangeLog rows written, self-relation rejected), `tests/vitest/actions/placement.test.ts` (Admin-only gating, single-placement `ADOPTED` flip with `placementGroupId` null, co-adoption sharing one `placementGroupId` and adopting all parties, ChangeLog rows on both `Placement` and `Animal`), plus new cases appended to the existing `tests/vitest/actions/animals.test.ts` for the ADOPTED-transition block (create and update), the already-ADOPTED-edit-passthrough, and FOSTER/PENDING_ADOPTION as ordinary transitions — **one pre-existing test in that file had to be fixed**, since it directly exercised an ACTIVE→ADOPTED `updateAnimal` transition that this session's own new business rule now correctly rejects. `tests/e2e/intake-groups-and-lineage.spec.ts` (7 tests): intake group creation + two-animal assignment + group detail listing both members, Admin-or-Shift-Lead-only assignment (Volunteer sees no form), a Shift Lead recording Guinness `SIRE_OF` Rowan with both directions checked in the browser, read-only relationship visibility for a Volunteer, a solo placement adopting an animal and freezing HHT Days at the placement date, co-adoption cross-linking two animals as "adopted together," and Foster/Pending-Adoption visibility under the existing "Show all" toggle. **One real fixture gotcha hit and fixed, the same one already documented in this file's V2 Session 4 entry:** the read-only-relationship test's first draft requested both `adminPage` and `volunteerPage` fixtures in one test (only `volunteerPage` was actually used — `adminPage` was dead weight left over from an earlier draft that fetched the recording Admin via the page instead of a direct Prisma query), which triggers Playwright's "you're already signed in" Clerk conflict since both fixtures share the same underlying `page`. Fixed by dropping the unused `adminPage` param.

## V2.md Session 7 — Admin Console (2026-07-19)

**Status: complete — this is the last session in V2.md's own 7-session plan.** Actions, UI, Vitest, and Playwright all built and green (230/230 Vitest, 70/70 Playwright, confirmed on a from-scratch `docker compose down -v` reset, not just an incremental run). No schema changes at all this session — every model this session touches (`EventCategory`, `VolunteerTag`, `CredentialType`, `TierThreshold`, `Location`, `ShiftTemplate`, `FarmSettings`, `Volunteer`) already existed from Sessions 1–6.

- **A deliberate, flagged deviation from V2.md's literal scope list, per this doc's own "note the deviation" instruction:** V2.md asks for "CRUD screens for: `EventCategory`, `VolunteerTag`, `TrainingRequirement` (i.e. `CredentialType`), `TierThreshold`, `Location`, `ShiftTemplate`, `FarmSettings`" all under one `/admin` area. Five of those seven (`VolunteerTag` on `/tags`, `CredentialType` on `/training`, `TierThreshold` on `/tiers`, `ShiftTemplate`+`FarmSettings` on `/settings`) already had full, working Admin-gated CRUD built in their own prior sessions — HANDOFF.md never flagged any of them as deferred to Session 7 the way it explicitly did for the other two. Only `Location` (full edit/deactivate — HANDOFF.md's Session 1 note: "full CRUD... is deferred to Session 7's Admin Console") and `EventCategory` (no CRUD at all yet — HANDOFF.md's Session 4 note: "deliberately deferred to Session 7's Admin Console, same as full Location CRUD") were actually missing. Rebuilding all seven under `/admin` would have meant duplicate parallel UI for five tables that already work, contradicting V2.md's own "no new business logic here" framing for this session. Instead: `/admin` is a real, Admin-only-gated console home that **links out** to the five already-working pages for those five tables, and gets **new dedicated screens** only for the two that were genuinely missing (`/admin/locations`, `/admin/event-categories`) plus the user-management piece that also didn't exist anywhere (`/admin/volunteers`). Flagging this explicitly in case the literal "rebuild all seven under `/admin`" reading was actually wanted.
- **`src/app/locations/actions.ts` gained `updateLocation`** (Admin-only): edits `name`/`isActive` plus the same per-type field set `createLocation` already validates (`fieldCode`/`turnoutOrder`/`bringInOrder` for `FIELD`, `barnNumber`/`stallNumber` for `BARN_STALL`) — `type` itself is not editable once a location exists, a narrower surface than create on purpose (changing a stall into a field with real assignment history would be a stranger operation than this screen needs to support). `createLocation` gained an optional `redirectTo` form field (same precedent Session 6 already set for `createFeedingOverride`/`createLocationAssignment`) so the Admin Console's own create form lands back on `/admin/locations` instead of the plain `/locations` list; the existing `/locations` page's own create form is unaffected (no `redirectTo` field, falls back to its original `/locations` target).
- **`src/app/admin/event-categories/actions.ts`** (new): `createEventCategory`/`updateEventCategory`, Admin-only, same lookup-table-config pattern as every other admin-managed table in this schema — not ChangeLog-tracked. This is genuinely new; nothing touched `EventCategory` writes anywhere before this session (V2.md Session 4 only seeded the six starting rows).
- **`src/app/admin/volunteers/actions.ts`** (new): `updateVolunteerRole` and `updateCanScheduleEvents`, both Admin-only, both routed through `withChangeLog` since `Volunteer` is already a tracked model — no new entries needed in `src/lib/prisma.ts`'s `trackedModels` array. Neither of these had a dedicated action anywhere before this session: `role` was previously only ever set once at signup via the Clerk webhook (defaulting to `VOLUNTEER`) with no in-app path to change it afterward, and `canScheduleEvents` (V2.md Session 4) could only ever be set by hand against the database. `releaseBlue`/`assignTag`/`removeTag` are **not** duplicated here — `/admin/volunteers` links to each volunteer's own `/volunteers/{id}` page (which already renders those Admin/Shift-Lead-gated controls) rather than rebuilding them a second time.
- **Route protection is real, not just a hidden nav item** — every `/admin/*` page (`/admin`, `/admin/locations`, `/admin/event-categories`, `/admin/volunteers`) uses the same try/catch-`requireRole(["ADMIN"])` pattern already established by the pre-existing `/admin` diagnostic page and `/animals/new` (render "Not authorized" and fetch zero admin data on failure, rather than a client-side redirect or a merely-hidden form). Per V2.md's own test-coverage ask ("actual route/API protection, not just hidden nav"), `tests/e2e/admin.spec.ts` checks this for a Shift Lead — the closest non-Admin role — across all four routes, not just checking that a button is invisible.
- **A real Playwright timing bug caught by running each new write-and-verify test repeatedly, not just once** (same "verify, don't assume" discipline every prior session's HANDOFF entry documents): the first draft of the role-change, `canScheduleEvents`-toggle, and Location-deactivate tests read the database immediately after `.click()` on a form's Save button. Since Playwright's `click()` does not reliably block until a plain `<form action={...}>` Server Action's POST-then-redirect has actually finished landing (this codebase's zero-client-JS forms are a real full-page navigation each time, not a fetch), those reads raced the write and flaked intermittently (roughly 1-in-3 runs) — confirmed by running the same test 3+ times in a row and seeing it flip between pass and fail with no code change in between. A second attempt tried waiting on the DOM instead (e.g. `expect(checkbox).toBeChecked()`) but that's a **false-positive wait**, not a real fix — `.check()`/`.selectOption()` set the element's client-side state immediately, before submission, so the assertion passes on the stale pre-submit DOM regardless of whether the server round-trip ever completes. The actual fix: `await Promise.all([page.waitForNavigation(), row.getByRole("button", { name: "Save" }).click()])`, which blocks on the real navigation event before either test moves on to a direct Prisma read. Confirmed stable across 3 consecutive full runs of `admin.spec.ts` (36/36) after the fix.
- **`src/app/admin/page.tsx`** replaces the diagnostic "am I admin" scaffold `CLAUDE.md`'s own Repo Layout note had already flagged as a stand-in ("treat this as a diagnostic scaffold, not a real admin dashboard — replace once real admin features exist"). `tests/e2e/smoke.spec.ts`'s old test asserting the literal diagnostic copy ("Admin access confirmed") was updated to check for the new console instead, rather than left stale alongside the new `admin.spec.ts` coverage.
- **Test coverage:** Vitest — `updateLocation` permission gating + per-type validation + the `redirectTo` override on `createLocation` (appended to `tests/vitest/actions/location.test.ts`), new `tests/vitest/actions/admin.test.ts` covering `createEventCategory`/`updateEventCategory` and `updateVolunteerRole`/`updateCanScheduleEvents` (permission gating, correctness, and — for the two `Volunteer` field writes — direct `ChangeLog` row assertions). `tests/e2e/admin.spec.ts` (12 tests): the four-route Shift-Lead block loop described above, a plain-Volunteer block on the console home, the console home's link inventory, EventCategory create+edit+deactivate, Location create+edit+deactivate, role change, `canScheduleEvents` grant, and the `/admin/volunteers` → `/volunteers/{id}` handoff for Blue release/tags.
- **Not applied to live Neon** — no migration this session at all (no schema changes), so nothing new is queued for the eventual cutover beyond the eleven migrations already pending from Sessions 1–6.
- **Per `V2.md`'s own rule, and because this was explicitly the last session in its plan: stopping here. There is no Session 8 — the V2 buildout described in `V2.md` is complete.** What comes after (live Neon cutover, Vercel deployment, the Phase 2 items `CONTEXT.md` §15 lists) is outside `V2.md`'s scope and needs its own explicit go-ahead from James.

## V2.md Session 6 — Dashboards: Feed Board & Turnout Board (2026-07-19)

**Status: complete.** Schema, actions, UI, Vitest, and Playwright all built and green (214/214 Vitest, 58/58 Playwright, confirmed on a from-scratch `docker compose down -v` reset, not just an incremental run). Per V2.md's own instructions, this session stops here — Session 7 (Admin Console) is not started.

- **A premise mismatch caught before building, not after:** V2.md's Turnout Board spec claims animals should be ordered within a field "reflecting herd hierarchy (lead animal at top) — this ordering decision was already made in an earlier session, carry it forward." No such field or decision exists anywhere in the schema, `CONTEXT.md`, or this file — checked directly before writing any code. Raised with James rather than assumed (same category as V2.md's Session 5 false claim about a pre-existing QR flow, which HANDOFF.md already flags). He confirmed the recommended fix: a new `Animal.herdOrder` field (nullable `Int`, admin-editable, global per animal, lower = higher in hierarchy, null sorts last with alphabetical tiebreak) — same simplicity precedent as `Location.turnoutOrder`/`bringInOrder`. Migration `20260719150000_add_herd_order`, purely additive (`ALTER TABLE "Animal" ADD COLUMN "herdOrder" INTEGER`), auto-generated then hand-renamed to sort after Session 5's migration timestamp (the auto-generated name sorted earlier in the same day, which would have been confusing in the migrations directory even though it caused no actual apply-order problem). Exposed as a new field on `AnimalFormFields.tsx` (Admin-only, same as every other core `Animal` field) and threaded through `createAnimal`/`updateAnimal`.
- **Schema/model reuse, no parallel tables:** per V2.md's own "reuse existing feeding baseline+override and care-entry models" instruction, neither board introduces a new write model — both are pure new read paths over `Animal`, `AnimalPhoto`, `FeedingBaseline`/`FeedingOverride`, `MedicationRegimen`, and `AnimalLocationAssignment`, all already built in prior sessions.
- **`src/app/feed-board/page.tsx`:** one row per `ACTIVE` animal — standardized headshot (primary `PROFILE` `AnimalPhoto`, `DISTINCT ON` per animal same pattern as the dashboard/locations pages), feed/hay amounts split into two columns by `FeedType.category` (`HAY` vs everything else), active medication regimen names (display-only, not editable — V2.md's edit list only names feed/hay amounts and special instructions), and special instructions (today's `FeedingOverride.notes` if logged, else `FeedingBaseline.notes` — same override-precedence pattern already used for amount). Each row links to the animal's current DAY location on `/turnout-board?period=DAY#location-{locationId}`.
- **`src/app/turnout-board/page.tsx`:** grouped by `Location`, day view = `type: FIELD` ordered by `turnoutOrder`, night view = everything else (`BARN_STALL`/`SICK_BAY`/`ARENA`/`OTHER`) ordered by type/barn/stall/name — a day/night toggle via `?period=` query param, matching the plain-link pattern already established by `/checkin`'s AM/PM jump links (no client JS). Animals within each location sort by `herdOrder` ascending, nulls last, alphabetical tiebreak.
- **Inline edit affordance, both boards:** V2.md's "breakpoint/role condition on the same view, not a separate route" is implemented as a pure CSS condition — the edit form is always server-rendered when the viewer is Admin/Shift-Lead, wrapped in a `hidden lg:flex` (or `lg:block`) Tailwind class so it only becomes visible at a desktop viewport width, with no client JS or separate route. Both boards reuse the **existing** write actions unchanged in shape (`createFeedingOverride`, `createLocationAssignment`) rather than adding parallel ones — both actions gained one new optional `redirectTo` hidden form field (defaulting to their original `/animals/{id}` target when absent, so the animal detail page's own forms and every existing test asserting that redirect target needed zero changes) so a board-triggered edit lands back on the board instead of navigating away to the animal's own page.
- **Mobile responsive behavior** ("TV/desktop expanded by default; mobile tap-to-reveal in portrait, no forced landscape," per V2.md — this specific claim of a "pattern already decided for this board" was also unverifiable anywhere in the repo, but unlike the herd-order case it's a fully-specified, non-ambiguous instruction on its own, so it was just implemented rather than raised): each Turnout Board location section renders its animal list twice — once in a `hidden lg:block` div (always-expanded desktop/TV) and once inside a `<details className="lg:hidden">` (native tap-to-reveal, collapsed by default on narrower viewports). Both are real DOM content simultaneously; the inactive one is fully `display:none`, not merely visually hidden — confirmed this matters for testing, see below.
- **Headshot square crop — first client component in the codebase.** V2.md explicitly calls for "a client-side crop step before the image goes to R2, not server-side resizing after the fact," which is unavoidably interactive/client-side; every other page in this app is zero-`"use client"` by deliberate convention (`CLAUDE.md`), so this is a narrow, spec-required exception rather than a drift in house style. `src/app/animals/[id]/HeadshotCropUpload.tsx`: a 320×320 `<canvas>` both renders the live preview and is the exact source of the uploaded blob (no separate higher-res recompute step to keep in sync — 320px comfortably exceeds every place this app currently displays a headshot, 64px on the Feed Board and 128px on the animal detail page). Image is scaled to cover the square (like CSS `object-fit: cover`) with up to two range-slider pan controls (only shown on whichever axis actually has slack, i.e. a portrait source only gets a vertical slider) — deliberately plain `<input type="range">` state rather than pointer-drag gesture handling, to keep the one client component in this codebase as simple as possible. On submit, `canvas.toBlob()` produces the cropped JPEG, posted via `fetch` (not a native form submission, since a `Blob` can't be attached to a native file input) to the **existing, unmodified** `/api/animals/[id]/photos` Route Handler — no new upload endpoint. `fetch` follows the handler's existing 303 redirect automatically, and `window.location.href = res.url` completes the navigation back to the animal page. The plain multipart form for non-headshot photo types (`MAP`/`PROGRESS`/`OTHER`) is unchanged and no longer offers a `PROFILE` option, since that path now always goes through the crop tool.
- **A real Playwright test-authoring gotcha, worth remembering for this responsive-duplication pattern specifically:** the desktop-expanded and mobile-`<details>` renderings both exist in the DOM at the same time (only one is `display:none` per breakpoint) — a plain CSS locator like `section.locator("li a")` matches both copies and returns duplicates, while a role-based query (`section.getByRole("link")`) resolves unambiguously because the browser's accessibility tree excludes `display:none` content entirely. First draft of the herd-order test used the CSS locator and got back six names instead of three before this was caught and fixed.
- **Two more test-authoring bugs caught by actually running the suite, not just writing it** (same "verify, don't assume" practice as every prior session): the edit-affordance test originally requested both `adminPage` and `openAs("volunteer")`-worthy access via two named page fixtures in one test — the exact `adminPage`+`volunteerPage`-in-one-test Clerk "already signed in" gotcha `events.spec.ts` hit before (both fixtures share Playwright's single per-test `page`) — fixed by using `openAs("volunteer")` instead of a second named fixture. The headshot upload test used `waitForURL` against the same URL pattern the page was **already on** before the upload (the crop tool starts and ends on `/animals/{id}`), so it resolved instantly without ever waiting for the upload to actually finish — replaced with an `expect(...).toBeVisible()` wait on the newly-uploaded photo actually appearing in the gallery, which only becomes true once the upload has genuinely completed.
- **Access:** both boards are read-only for any signed-in volunteer (`requireVolunteer()`, no role check) — same precedent as `/dashboard` and `/locations`. V2.md's "read-only for everyone on the TV display" is read as "read-only for every role," not "no login required" — unlike `/kiosk`, nothing in V2.md's Session 6 text says "no login required on the shared device," so this stays behind the same auth every other board/report page in this app already requires. Flagging this interpretation explicitly in case a genuinely unauthenticated TV display turns out to be wanted later.
- **Test coverage:** Vitest — `herdOrder` persistence on create/update plus its ChangeLog diff (`tests/vitest/actions/animals.test.ts`), the new `redirectTo` override behavior on `createFeedingOverride`/`createLocationAssignment` (`feeding.test.ts`/`location.test.ts`). No dedicated Vitest file for either board's own `page.tsx` — matching the precedent already set by `/dashboard` and `/locations` (pure new read paths with no new Server Actions of their own get Playwright coverage, not Vitest, in this codebase). `tests/e2e/boards.spec.ts` (7 tests): Feed Board data + override precedence across feed/hay/meds/instructions, the Feed Board → Turnout Board link, herd-hierarchy ordering (inserted out of order to prove it's not insertion-order), the day/night toggle, edit-affordance role+breakpoint gating on both boards, mobile tap-to-reveal vs desktop-expanded, and the headshot crop producing a square image from a non-square (400×200) source — verified via `img.naturalWidth === img.naturalHeight` after upload, using an in-browser `<canvas>`-generated PNG rather than a checked-in fixture file.
- **Not applied to live Neon** — same holding pattern as every V2 migration before it; now eight migrations deep queued for the coordinated cutover (see "What's Next").
- **Per `V2.md`'s own rule, stopping here — do not start Session 7 (Admin Console) until James starts a new session for it.**

## V2.md Session 5 — Shift Templates, Seasonal Hours & Sign-In/Out (2026-07-19)

**Status: complete.** Schema, migration, lib logic, actions, UI, Vitest, and Playwright all built and green (210/210 Vitest, 51/51 Playwright, confirmed on a from-scratch `docker compose down -v` reset, not just an incremental run). Per V2.md's own instructions, this session stops here — Session 6 (Dashboards: Feed Board & Turnout Board) is not started.

- **Winter offset confirmed directly with James before touching seed data**, per V2.md's own "confirm rather than guess" flag: AM Shift moves later (09:00–11:00 → 10:00–12:00, later sunrise), PM Shift moves earlier (16:00–19:00 → 15:00–18:00, earlier sunset) — see `prisma/seed.ts`.
- **Schema:** `FarmSeason` enum (`STANDARD`/`WINTER`), `FarmSettings` (singleton, enforced app-side via `getFarmSettings()` in `src/lib/farmSettings.ts` — find-first-or-create, not a DB constraint, same pattern as everything else admin-entered in this schema), `ShiftTemplate` (`standardStartTime`/`standardEndTime`/`winterStartTime`/`winterEndTime` as plain `"HH:MM"` strings — deliberately not `@db.Time`, nothing here ever does arithmetic on them and a native `<input type="time">` round-trips a string cleanly), `Shift.actualStartTime`/`actualEndTime` (nullable per-occurrence override), `Volunteer.checkInCode` (`@unique @default(cuid())`). Migration `20260719140000_shift_templates_seasonal_hours_and_kiosk`, **hand-written, not auto-generated** — the local test DB had 19 pre-existing `Volunteer` rows (leftover E2E test users), so `checkInCode` couldn't be added as a plain required column with a Prisma-level default in one step (Prisma's `cuid()`/`uuid()` defaults are client-side only, never pushed to Postgres as a `DEFAULT`). Added nullable, backfilled with `gen_random_uuid()::text` (built into Postgres core since v13, no extension needed), then tightened to `NOT NULL UNIQUE` — same technique already used in this repo for the `Location.fieldCode` `NOT NULL` fix. Verified zero drift via `prisma migrate diff --from-config-datasource --to-schema` before trusting it, and re-confirmed the whole migration set applies cleanly on a completely fresh `docker compose down -v` container (no existing rows to backfill in that case, but the SQL path is exercised either way).
- **`ShiftTemplate` deviates from V2.md's literal field list in one way, flagged per this doc's own instruction:** added `shiftType: ShiftType @unique`, tying each template row directly to the existing `ShiftType` enum (AM/PM) rather than matching on `name` — needed so a `Shift.type` can look up its template without a fragile string match. Since `ShiftType` is a fixed two-value hard enum (`CONTEXT.md` §3), this caps `ShiftTemplate` at exactly two rows forever — treated as **edit-only, no create/delete**, same precedent as `TierThreshold`'s fixed four rows. Neither `FarmSettings` nor `ShiftTemplate` is ChangeLog-tracked (admin config/lookup category, same as `TierThreshold`/`Location`) — `Shift` **is** newly tracked (added to `trackedModels` in `src/lib/prisma.ts`), since its new `actualStartTime`/`actualEndTime` fields are real mutable data, not lookup config; who/when is captured by ChangeLog rather than a dedicated field, matching the `PastureAssignment` precedent `CONTEXT.md` §10 already documents.
- **Seasonal resolution logic lives in `src/lib/shifts.ts`**, pure and side-effect-free (same "extract it so the UI and tests share real logic" pattern as `src/lib/tier.ts`/`src/lib/locations.ts`): `resolveShiftTimes()` (template + season → window, falling back to standard if a template has no winter times configured), `resolveShiftTimesForOccurrence()` (an occurrence's `actualStartTime`/`actualEndTime` wins over the template), and `determineShiftTypeForNow()` (which shift is "in progress" right now, for the kiosk's no-questions-asked toggle — inside a resolved window picks that shift; in the gap between AM's end and PM's start, splits at the midpoint; before/after both windows, picks whichever is closer). All three have direct Vitest coverage (`tests/vitest/lib/shifts.test.ts`) independent of any DB.
- **Per-occurrence override (`setShiftActualTimes` in `src/app/checkin/actions.ts`):** Admin-or-Shift-Lead, for **any** date+type occurrence, not scoped to a specific person's own assignment — V2.md's "their own shift" phrasing was ambiguous (no formal ownership FK exists between a Shift Lead and a `Shift` occurrence), so this session's own call was to treat it the same as `CareEntry`/`FeedingOverride`: Shift Lead already has org-wide read access to shifts per `CLAUDE.md`'s permission table, and this is a simple same-day correction, not a restricted-to-their-assignment action. "ADMIN can override a shift lead's override" turned out to need no special locking — both roles just have write access, whoever saves last wins, verified in both Vitest and a real two-actor Playwright flow (`shiftLeadPage` sets it, then `openAs("admin")` overwrites it).
- **Kiosk / QR real-time toggle — the actual new capability, additive to the existing retrospective `WEB_FORM` flow at `/checkin`, not a replacement** (`CONTEXT.md` §8 had already anticipated this: "the same shape supports true real-time check-in/out later ... without a schema change"). **Flagging a premise mismatch in V2.md itself:** its test-coverage list asks for "regression coverage confirming the existing personal-device QR flow still works unchanged" — no QR flow existed anywhere in this codebase before this session (only the `CheckMethod` enum's `QR` value was pre-modeled, unused). Built fresh this session, not a regression risk on prior work.
  - `src/lib/checkin.ts`'s `performKioskToggle(code, now)` is the actual logic, deliberately **not date-scoped**: it closes the volunteer's most recent *any-date* open `CheckIn` if one exists, or opens a new one if not. This also self-heals a forgotten checkout from a prior day rather than leaving it open forever — covered directly in Vitest (`tests/vitest/actions/kiosk.test.ts`). Shift type for a fresh check-in is resolved automatically via `determineShiftTypeForNow()` — no AM/PM prompt at the kiosk, keeping the walk-up flow to one field (the code) and one button.
  - `src/app/kiosk/page.tsx` + `actions.ts`: **deliberately no `requireVolunteer()`/`requireRole()` call anywhere in this path** — the entire point (V2.md: "no login required on the shared device itself"). Safe because `src/proxy.ts` already enforces nothing at the middleware layer (`CLAUDE.md` already documents this — every other page's protection is self-contained in its own `requireVolunteer()` call, so an unprotected page is consistent with the existing architecture, not a new hole). The volunteer is identified purely by `checkInCode`.
  - `src/app/checkin/code/page.tsx`: the "personal-phone QR flow" half — authenticated, shows the volunteer's own `checkInCode` as an inline server-rendered SVG QR (via the new `qrcode` npm dependency, `QRCode.toString(url, { type: "svg" })`) encoding a link to `/kiosk?code=...`. **Zero client JS** — matches this codebase's existing convention of zero `"use client"` components anywhere; the QR is generated server-side per request and embedded as static markup, same simplicity bias as everything else in this app.
  - **A real timezone bug caught and fixed before it shipped:** `performKioskToggle`'s first draft built "today" from local `Date` components (`new Date(date.getFullYear(), date.getMonth(), date.getDate())`), while `submitCheckIn` and the checkin page both already used a UTC-date-string convention (`new Date().toISOString().slice(0, 10))`. In any non-UTC timezone (this rescue is in Tennessee — US Central/Eastern), a kiosk check-in near midnight local time could land on a different calendar day than a same-day web-form check-in, silently creating two `Shift` rows for what a human would call "today." Fixed by making `performKioskToggle`'s `startOfDay()` use the same UTC-string parse everywhere. Also had to fix `src/app/checkin/page.tsx` itself, which had drifted into the same local-vs-UTC mismatch between its date-input default and its "today's shift" query — both now derive from one `todayString` value, parsed once.
- **Check-in page changes:** pre-fills Time In/Time Out from the resolved shift window (`resolveShiftTimesForOccurrence`) — AM/PM radio buttons are unchanged from before (kept them, rather than replacing with JS-driven live switching, specifically so the existing E2E assertions like `getByLabel("AM", { exact: true }).check()` keep working — this codebase has zero client components anywhere and this session didn't want to be the first to introduce one for a cosmetic convenience). Two plain links ("Jump to AM / PM") reload the page with `?shiftType=` to change which shift's time gets pre-filled server-side; the radios are still what actually gets submitted. New "Today's shift times" reference table below the form shows both AM/PM resolved times, with the Shift-Lead/Admin-only override form inline. New "My check-in code / QR" link to `/checkin/code`.
- **`/settings` page:** active-season toggle (Admin writes, everyone reads) plus the two-row `ShiftTemplate` edit table (Admin-only, same `/tiers`-style inline-edit-per-row pattern).
- **Test coverage:** `tests/vitest/lib/shifts.test.ts` (pure resolution logic, no DB), `tests/vitest/actions/settings.test.ts` (`updateFarmSettings`/`updateShiftTemplate` permission gating + edits, with explicit restore-after since both tables are lookup/config and never truncated between tests), `tests/vitest/actions/kiosk.test.ts` (unknown code, toggle open/close, no-duplicate-open-sessions, cross-day self-heal, `firstShiftDate` set on a kiosk check-in same as the web form, ChangeLog capture), new `setShiftActualTimes` tests appended to `tests/vitest/actions/checkin.test.ts` (permission gating, Shift-row reuse, Admin-overwrites-Shift-Lead). `tests/e2e/shifts-and-kiosk.spec.ts` (9 tests): template edit, season flip changing resolved times on `/checkin`, two-actor override/overwrite flow, read-only volunteer view, full kiosk check-in→check-out round trip with **no `signIn()` call at all** (genuinely unauthenticated), unrecognized-code error, the QR-linked toggle path, and a regression check on the existing retrospective web-form flow. `ShiftTemplate`/`FarmSettings` added to both Vitest's and Playwright's lookup-table (never-truncated) sets in their respective `helpers/db.ts`.
- **Not applied to live Neon** — same holding pattern as every V2 migration before it; now seven migrations deep queued for the coordinated cutover (see "What's Next").
- **Per `V2.md`'s own rule, stopping here — do not start Session 6 (Dashboards: Feed Board & Turnout Board) until James starts a new session for it.**

## V2.md Session 4 — Event Scheduler & Signup (2026-07-19)

**Status: complete.** Schema, actions, UI, Vitest, and Playwright all built and green. Per V2.md's own instructions, this session stops here — Session 5 (Shift Templates, Seasonal Hours & Sign-In/Out) is not started.

- **Schema:** `EventCategory` (lookup, seeded with the six categories V2.md names), `Event`, `EventWatcher`, `EventSignup` (`EventSignupStatus`: `CONFIRMED`/`WAITLISTED`/`CANCELLED`), plus `Volunteer.canScheduleEvents`. Migration `20260719113145_event_scheduler_and_signup`, hand-verified as purely additive (no drops/renames) before it touched anything — generated and applied against the local Docker test DB only, **not yet run against Neon** (same deferred-until-confirmed pattern as every schema change since the `Horse`→`Animal` rename; see "What's Next" below). None of these four models are in `trackedModels` — no legal-defensibility angle on scheduling data, same reasoning as `Location`.
- **Gating interpretation (flagged, not confirmed):** an event can set `requiredTagId`, `requiredTier`, both, or neither. V2.md's only worked example (Blue Handler Class) sets just `requiredTier`, so the both-set case is this session's own call: **AND**, not OR — a volunteer must clear both bars, not either one. `requiredTier` is a floor (`tierAtLeast()`, new in `src/lib/tier.ts`), not an exact match — a Blue-released volunteer still qualifies for a Yellow-gated event. Worth confirming with James/Lori if a real event ever needs both fields set.
- **Capacity/waitlist:** one `EventSignup` row per (event, volunteer) — `@@unique([eventId, volunteerId])` — re-signing up after a cancellation flips the same row's status back rather than inserting a duplicate, refreshing `signedUpAt` so promotion order reflects the current queue position. Canceling a `CONFIRMED` signup promotes the earliest `WAITLISTED` row (`orderBy signedUpAt asc`); canceling a `WAITLISTED` signup promotes no one. Verified both branches in Vitest and in a real 3-actor Playwright flow.
- **First real use of Resend in this codebase** (`src/lib/email.ts`) — CONTEXT.md §7 had earmarked it for credential/training expiration reminders, but nothing had actually wired it up before this session. **Found the hard way:** a module-level `throw` on a missing `RESEND_API_KEY` (mirroring `src/lib/r2.ts`'s pattern) broke `next build` outright — Next evaluates route modules at build time, and `RESEND_API_KEY` turned out to be present-but-blank in this project's `.env` (never actually filled in). Fixed by making `sendEmail()` degrade to a logged no-op instead of throwing, both for a missing key and for a failed send — notification delivery is a courtesy, not a transactional guarantee this app can promise on top of a free-tier provider, and a volunteer's signup/cancellation must succeed either way. **Update, same day:** James created a real Resend API key for volunteerOps and filled in `.env`. Re-ran `tests/e2e/events.spec.ts` directly against it — all 6 tests passed with no `sendEmail skipped`/`sendEmail failed` log lines, confirming Resend genuinely accepts the sends now. The graceful-degradation behavior in `sendEmail()` was kept rather than reverted to a hard throw — a missing key in some *other* environment (fresh clone, wiped `.env`) still shouldn't be able to take down the whole app over an optional notification feature. `EMAIL_FROM` is still unset (sends from Resend's shared sandbox address, not a verified `horsehaventn.org` sender) — fine short-term, worth adding before this is relied on for real.
- **Permissions:** `createEvent` requires `canScheduleEvents` or `ADMIN`; `updateEvent`/`cancelEvent` are further scoped to that event's own creator or an `ADMIN` (one `canScheduleEvents` volunteer can't edit another's event — not explicitly specified by V2.md, but felt like the safer default than any-organizer-edits-any-event). Signup/cancel is self-service only, no admin-on-behalf-of-someone-else path, matching the same MVP scope cut already made for `CredentialRecord`/training self-attestation.
- **`EventCategory` has no CRUD UI yet** — deliberately deferred to Session 7's Admin Console, same as full `Location` CRUD. The six seeded categories (Fall Festival, Outreach/Tabling, Meetup, Training Class, General Volunteer Shift, Other) cover this session's scope.
- **A real Playwright fixture gotcha, worth remembering for future multi-actor tests:** `adminPage`/`shiftLeadPage`/`volunteerPage` all resolve to the *same* underlying built-in `page` fixture within one test (Playwright dedupes fixture dependencies), so a test that requested two of them (e.g. `{ adminPage, volunteerPage }`) failed with Clerk's "You're already signed in" — the second fixture's sign-in ran against the same already-signed-in page as the first. `fixtures.ts`'s `openAs(role)` exists precisely for this (a fresh browser context) and its own comment already said so — first attempt at `events.spec.ts` didn't follow that pattern and had to be fixed. Also hit one real cross-page race: canceling a signup on `volunteerPage` and immediately reloading `shiftLeadPage` to check the waitlist promotion needs an explicit wait on `volunteerPage` first (e.g. the "Sign up" button reappearing) — two independent pages have no shared navigation to implicitly wait on, unlike same-page checks elsewhere in this suite where Playwright's auto-retry absorbs the delay.
- **Result:** `npm run test:db:reset && npm run test:unit` → **174/174 passing** (31 new: `tests/vitest/lib/events.test.ts` + `tests/vitest/actions/events.test.ts`), `npm run test:e2e` → **41/42 passing** (6 new in `tests/e2e/events.spec.ts`, all passing on both an isolated run and the full-suite run) — the one failure (`locations.spec.ts`'s day/night independent-tracking test) is a pre-existing flake unrelated to this session's changes, confirmed by re-running that spec alone (5/5 green). `tsc --noEmit`/`npm run lint` both clean. Confirmed on a from-scratch `docker compose down -v` reset, not just an incremental run.



## Current State, in One Paragraph

The repo is scaffolded, connected to a real Neon database, real Clerk auth, and a real R2 bucket — none of this is theoretical, all of it has been exercised end to end with real data (though **the eight schema migrations covering the Horse→Animal rename through V2.md Session 6 are not yet applied to Neon** — see "What's Next"; Session 7 added no migration at all). Eight Phase 1 feature slices are built and verified working: authentication (Clerk → webhook → `Volunteer` row → role-based access), check-in (volunteers logging shifts), horse core records with photo upload, feeding (baseline + daily override), medication (regimen + daily log), care/health (care entries + ongoing health issues), metrics (weight entries + generic Henneke BCS/height metrics), and location assignment (move a horse between fields/stalls, day and night tracked independently, plus a plain locations list — V2.md Session 1, generalized from the original field/pasture-only design). On top of those, the cross-entity daily dashboard (`/dashboard`) ties them together in one read-only view, V2.md Session 2 added the Green→Orange→Yellow→Blue tier progression system plus compliance training tracking (`/volunteers`, `/tiers`, `/training`), V2.md Session 3 added generic volunteer tagging with Go Team as the first real tag plus an "eligibility candidates" report (`/tags`), V2.md Session 4 added self-service event scheduling and signup with capacity/waitlist handling and tag/tier-gated events (`/events`) — the first real use of Resend in this codebase, confirmed actually sending once James filled in a real `RESEND_API_KEY` (see the Resend row below) — V2.md Session 5 added shift templates with seasonal (standard/winter) reference times, a per-occurrence actual-time override, and a real-time kiosk/QR check-in-and-out toggle alongside the existing retrospective form (`/settings`, `/kiosk`, `/checkin/code`), V2.md Session 6 added two read-only large-screen boards (`/feed-board`, `/turnout-board`) built entirely on data already captured by prior sessions, plus this codebase's first client component (a square headshot crop tool), and **V2.md Session 7 — the last session in the V2 plan — added the real Admin Console** (`/admin`, `/admin/locations`, `/admin/event-categories`, `/admin/volunteers`), replacing the old diagnostic "am I admin" page and giving Admins routine data control (Location/EventCategory full CRUD, volunteer role changes, `canScheduleEvents` toggling) without needing a developer. **The V2.md buildout plan is now complete end to end.** Nothing is deployed yet — this has all been run and tested locally against a throwaway Postgres (via Playwright's own `webServer`) and, for earlier slices, via `npm run dev` tunneled through ngrok for the Clerk webhook. Not yet on Vercel.

## V2 Rename Session — `Horse` → `Animal` (2026-07-18)

Before starting `V2.md` Session 1 (the Location model generalization), V2.md's own terminology note called for the core entity to be `Animal` rather than `Horse`, to cover mules, donkeys, minis, ponies, and non-equine animals — but the entire codebase up to this point was built around `Horse`. Rather than guess, this was raised with James directly: full rename now, as its own dedicated session separate from Location work, with a `species` enum added at the same time. Full detail on the reasoning is in `CONTEXT.md` §18; this is what actually happened.

- **Schema:** `Horse` → `Animal`, `HorseStatus` → `AnimalStatus`, `HorseSex` → `AnimalSex`, `HorsePhoto` → `AnimalPhoto`, `HorseMetric` → `AnimalMetric`, every `horseId` FK → `animalId` (including `DonationInKind.linkedHorseId` → `linkedAnimalId`). New `AnimalSpecies` enum (`HORSE | DONKEY | MULE | MINI_HORSE | PONY | CAT | OTHER`), new `Animal.species` field defaulting to `HORSE`.
- **Migration was hand-written**, not auto-generated — `prisma migrate dev`'s non-interactive diff warned it would drop the live `Horse` table (1 row in the local test DB, and real data in Neon), since it can't infer a rename without a prompt. The actual migration (`prisma/migrations/20260718223633_rename_horse_to_animal/migration.sql`) uses `ALTER TABLE/COLUMN/TYPE ... RENAME` throughout — verified with `prisma migrate diff --from-config-datasource --to-schema` afterward, which reported zero drift, before it was ever pointed at production.
- **Binx backfill:** queried the live Neon DB directly (read-only) for every `Horse` row before writing the migration. Found exactly one record named `Binx` (`sex: GELDING`, `status: ACTIVE`, no notes) — confirmed with James this was the barn cat before the migration's `UPDATE "Animal" SET species = 'CAT' WHERE name = 'Binx'` was written. The other three real records (Remus, Aries, Prada) were left as `HORSE`.
- **Code:** `src/app/horses/` → `src/app/animals/` (directory rename, including `HorseFormFields.tsx` → `AnimalFormFields.tsx`), `src/app/api/horses/[id]/photos/` → `src/app/api/animals/[id]/photos/`, `createHorse`/`updateHorse` → `createAnimal`/`updateAnimal`, `createHorseMetric` → `createAnimalMetric`, `src/lib/prisma.ts`'s `trackedModels` array updated. R2 storage key prefix for photo uploads changed from `horses/<id>/...` to `animals/<id>/...` for new uploads (old photos keep their already-stored full URLs, so nothing broke there).
- **User-visible copy deliberately did not change** — nav links, page headings ("Horses", "Add horse", "Create horse"), and button text still say "Horse" throughout, matching V2.md's own stated convention that staff-facing language stays "horse" even though the schema says `Animal`. Only routes, file paths, and code identifiers moved.
- **`ChangeLog` history was left as-is** — old rows say `entityType: "Horse"`/`"HorseMetric"` since that was accurate when they were written (§4's append-only principle); only new writes use `"Animal"`/`"AnimalMetric"`.
- **Both test suites updated and fully green**: `tests/vitest/actions/horses.test.ts` → `animals.test.ts`, `tests/e2e/horses.spec.ts` → `animals.spec.ts`, plus every other test file/helper touching `horseId`/`createHorse`/`prisma.horse*`. Confirmed via `npm run test:db:reset && npm run test:unit` (81/81 passing) and `npm run test:e2e` (21/21 passing) after the rename — not just typecheck/lint. `npx tsc --noEmit` and `npm run lint` also both pass clean (had to clear a stale `.next/` build cache first, which still referenced pre-rename paths from an earlier `next dev` run).
- **No new feature work rode along with this** — no species picker in the UI, no species column shown anywhere yet. `species` is populated correctly in the schema and migration; surfacing it is left for whenever it's actually needed.
- **Not yet applied to the live Neon database** — this was deliberately held back for a separate, explicit confirmation before running `prisma migrate deploy` against production, since it's a real schema change plus a data backfill on live records. See "What's Next" below.

## Commit History (14 commits on `main`, all pushed — this list had gone stale for several sessions; corrected 2026-07-18)

```
81285ba Scaffold Next.js + Prisma project from grill session decisions
d3d95e8 Fix Neon pooled/direct connection split and stop ignoring .env.example
22c4328 Move seed command config to prisma.config.ts per Prisma 7
5cbe0c2 Wire up Clerk webhook, sign-in UI, and first protected page
794a81b Add check-in feature - first real Phase 1 slice
b034665 Add initial migration history
5d70aa6 Add Horse core CRUD - second Phase 1 slice
745a33b Add horse photo upload via R2 - third Phase 1 slice
21f7e7c Add feeding baseline/override - fourth Phase 1 slice
1b4f250 Add HANDOFF.md - session continuity log
0f32fc3 feat: medication and care/heatlh tracking
b2a7a5d feat: added Horse Metrics and Field/Pasture Assignment
fc94ce1 Building out the ops platform's daily dashboard
a209c37 build out test suite: Playwright e2e + Vitest API + db
```

Read the commit messages themselves for detail — each one documents the reasoning for that slice, not just what changed.

**Workflow note for continuing this pattern:** Claude (in claude.ai, not Claude Code) has no push access to GitHub in this environment. Every commit above was built and committed locally by Claude, packaged as a `git format-patch` file, downloaded, and applied via `git am <patch>` — which applies *and commits* in one step, preserving the original message. If a new chat session continues this way, expect the same pattern: Claude builds and commits in its own sandbox, hands over a `.patch` file, you `git am` it and `git push`. If you're instead using Claude Code directly against this repo, it should have real push access and this whole dance is unnecessary — just let it commit and push directly.

## File Tree (everything under `src/` and `prisma/`, as of the original test-suite session — a historical snapshot, not maintained)

**Stale as of the `Horse`→`Animal` rename and everything after it** (`/horses` → `/animals`, `pasture-actions.ts`/`fields/page.tsx` → `location-actions.ts`/`locations/`, plus everything V2.md Session 2 added — `/volunteers`, `/tiers`, `/training`, `src/lib/tier.ts`, `src/lib/training.ts`, `src/lib/locations.ts`). `CLAUDE.md`'s Repo Layout section is the actively-maintained current file listing — treat the tree below as "what this looked like the night the test suite was first built," kept for that historical context rather than rewritten every session.

```
prisma/schema.prisma
prisma/seed.ts
src/app/admin/page.tsx                        — diagnostic "am I admin" page, not a real feature
src/app/api/horses/[id]/photos/route.ts       — photo upload Route Handler
src/app/api/webhooks/clerk/route.ts           — Clerk user lifecycle webhook
src/app/checkin/actions.ts
src/app/checkin/page.tsx
docker-compose.test.yml                       — throwaway local Postgres for tests, tmpfs-backed, port 5433
.env.test.example                             — checked-in template; copy to .env.test (gitignored, no secrets needed)
vitest.config.ts
playwright.config.ts                          — updated: globalSetup, serial workers, reuseExistingServer:false (safety)
tests/vitest/setup.ts                         — Clerk/next-navigation mocks + per-test DB reset
tests/vitest/helpers/{db,factories,form,auth-mock,signals}.ts
tests/vitest/lib/{auth,changelog}.test.ts
tests/vitest/actions/{checkin,horses,feeding,medication,care,metrics,pasture}.test.ts
tests/vitest/routes/{photos,clerk-webhook}.test.ts
tests/e2e/test-users.ts                       — the 3 fixed e2e-*@volunteer-ops.test Clerk test users
tests/e2e/global-setup.ts                     — provisions those users via Clerk Backend API + seeds matching Volunteer rows
tests/e2e/fixtures.ts                         — adminPage/shiftLeadPage/volunteerPage/openAs(role)
tests/e2e/helpers/db.ts
tests/e2e/{smoke,checkin,horses,feeding,pasture,care-and-metrics,dashboard}.spec.ts
src/app/dashboard/page.tsx                    — cross-entity daily dashboard, one row per active horse, read-only
src/app/fields/page.tsx                       — plain fields list (V1 in place of Phase 2 map), read-only
src/app/horses/HorseFormFields.tsx            — shared create/edit field set
src/app/horses/[id]/care-actions.ts            — createCareEntry/createHealthIssue/resolveHealthIssue
src/app/horses/[id]/edit/page.tsx
src/app/horses/[id]/feeding-actions.ts
src/app/horses/[id]/medication-actions.ts     — createMedicationRegimen/endMedicationRegimen/logMedicationAdministered
src/app/horses/[id]/metrics-actions.ts        — createWeightEntry/createHorseMetric
src/app/horses/[id]/page.tsx                  — detail page: core fields, photos, feeding, medication, care/health, metrics, field/pasture
src/app/horses/[id]/pasture-actions.ts        — assignPasture
src/app/horses/actions.ts                     — createHorse/updateHorse
src/app/horses/new/page.tsx
src/app/horses/page.tsx                       — list, defaults to ACTIVE only
src/app/layout.tsx                            — ClerkProvider wrapper
src/app/page.tsx                              — homepage, sign-in + nav links
src/lib/auth.ts                               — getCurrentVolunteer/requireVolunteer/requireRole
src/lib/prisma.ts                             — Prisma client + withChangeLog extension
src/lib/r2.ts                                 — R2 S3-compatible client
src/proxy.ts                                  — Clerk middleware (Next 16 naming)
```

## What's Actually Live in Neon Right Now

Real data exists from testing, not just seed data: your own `Volunteer` row (role `ADMIN`, tier `GREEN`), at least one test `Horse` with uploaded photos, at least one `CheckIn`, at least one `FeedingBaseline` + `FeedingOverride`, and a substantial `ChangeLog` history from all of the above (every create/update on a tracked model logs field-by-field, so the row count there is much higher than it looks at first glance — that's expected, see `CLAUDE.md`). Medication and care/health writes were verified against this same live DB with a throwaway script (create → read the exact page.tsx query shapes → update → hard-delete the test rows via raw Prisma, bypassing `withChangeLog` on purpose so no test noise landed in `ChangeLog`) — no leftover test data, real `Horse`/`Volunteer` rows untouched.

Seeded lookup tables (via `npm run db:seed`, already run): `CredentialType`, `FeedType`, `CareType`, `WorkType`, `MetricType`, `Field` (all 14 field codes from the map).

## External Services — Setup Status

| Service | Status | Notes |
| --- | --- | --- |
| **Neon** | Live, migrated, seeded | Project connected via both pooled (`DATABASE_URL`) and direct (`DIRECT_URL`) connection strings. Compute scales to zero after inactivity — expect the first query/migration after a gap to fail once and succeed on retry (`P1017: Server has closed the connection`). This happened three separate times tonight (twice on `migrate dev`, once opening Prisma Studio) and a plain retry fixed it every time. Not a bug, just how Neon's free tier behaves. |
| **Clerk** | Live, dev instance | App named `volunteer-ops`, separate from Shenny/testLens. Webhook endpoint configured for `user.created`/`user.updated`/`user.deleted`, currently pointed at an **ngrok tunnel URL, which changes every time ngrok restarts.** Starting a new session that needs webhook testing (new signups, not just using an already-linked account) means: start ngrok fresh, copy the new forwarding URL, go to Clerk dashboard → Webhooks → your existing endpoint → update the URL to match. The signing secret does NOT change when you update the URL on an existing endpoint — only if you delete and recreate the endpoint. Using Clerk with development keys — production keys are a separate, later step (Clerk will nag about this, it's expected at this stage). |
| **Cloudflare R2** | Live | Bucket `volunteer-ops`. Public Development URL enabled (the `pub-xxxx.r2.dev` domain) — **Cloudflare's own docs say this is rate-limited and dev-only; a custom domain is the recommended production path.** Not urgent, but on the list before real deployment. API token: Account API token, Object Read & Write, scoped to this bucket specifically (not Admin, not account-wide — least privilege, deliberate). |
| **GitHub Actions secrets** | **Not configured yet** | `nightly-backup.yml` still references repo secrets (`DATABASE_URL`, `DIRECT_URL`, R2 creds) that don't exist yet — still red/failing, still needed before the automated backup actually runs. `ci.yml` changed this session: it no longer touches Neon at all (lint/typecheck/Vitest/Playwright all run against a throwaway Postgres service container), so it only needs `CLERK_PUBLISHABLE_KEY`/`CLERK_SECRET_KEY` as repo secrets now — those are what E2E's Clerk sign-in needs. Also not configured yet, so CI's E2E step will fail until they're added, but that's a smaller/safer gap than before (a missing secret fails loudly; it can no longer silently point CI at production data). |
| **Vercel** | Not set up | This project has only ever run via `npm run dev`. No deployment yet. |
| **Resend** | **Live** | `RESEND_API_KEY` is a real key now (created for volunteerOps in Resend, added by James right after Session 4 flagged it as blank). Re-ran `tests/e2e/events.spec.ts` against it directly — all 6 tests passed with no `sendEmail skipped`/`sendEmail failed` log lines, confirming Resend is genuinely accepting sends now (the E2E test users are disposable `*.example.com` addresses, so nothing lands in a real inbox, but the API call itself succeeds). `EMAIL_FROM` is still unset, so mail sends from Resend's shared sandbox address rather than a verified `horsehaventn.org` sender — fine short-term, worth a real domain before this is relied on in production. |

## Real Bugs Found Tonight (read before touching related code)

This stack runs newer package versions than typical AI training data assumes, and several genuine version-specific traps got caught *before* they became silent failures, by verifying against current docs rather than assuming. Full detail on each is in `CLAUDE.md`; short version here for orientation:

- **Prisma 7** removed `$use` middleware (Client Extensions are the only hook mechanism now — this is what `withChangeLog` relies on), changed the generator provider name and requires an explicit `output` path, requires a driver adapter passed to `PrismaClient`'s constructor, moved connection config to `prisma.config.ts`, and moved the seed command from `package.json` into `prisma.config.ts`'s `migrations.seed` field. Every one of these was a real error message hit and fixed during setup, not a precaution.
- **Neon + Prisma 7** needs two separate connection strings (pooled for the app, direct/unpooled for migrations and the seed script) — collapsing them back into one breaks migrations via PgBouncer's transaction-pooling mode.
- **Next.js 16** renamed `middleware.ts` to `proxy.ts`, made `params`/`searchParams`/`cookies()` all return Promises, and — this one's still an open risk, not fully resolved — May 2026's security patch (fixing a real DoS CVE) introduced a regression where Server Actions using `useActionState` can receive empty `FormData`. Mitigation in place: every form in this app uses a plain `<form action={...}>` without `useActionState`. This worked fine in testing tonight, but it's not a guarantee the underlying issue is fully absent from our exact patch version — if a future form's submitted values come back empty/null unexpectedly, this is the first thing to suspect.
- **Server Actions cap request bodies at 1MB by default**, and the documented workaround (`bodySizeLimit` in `next.config.ts`) has a long, messy history of not reliably working across versions and hosts. This is *why* photo upload was built as a Route Handler with a plain multipart form instead of a Server Action — sidesteps the whole problem, and it's current official guidance, not a workaround of our own invention.
- **Clerk v7**: `auth()` is async and returns `isAuthenticated` explicitly (don't trust bare `userId` truthiness), and `SignedIn`/`SignedOut` don't exist anymore — replaced by a single `<Show when="signed-in">` component.
- **R2 has two different token systems** that both mention R2 permissions, and only one of them actually produces usable S3-compatible credentials. The general Cloudflare API Token builder (My Profile → API Tokens → Custom Token, "Workers R2 Storage" permission group) gives you a token value that is **not** directly usable as an S3 secret key — it needs to be SHA-256 hashed first, which that flow doesn't do for you. The dedicated R2 → Manage R2 API Tokens flow gives you a ready-to-use Access Key ID/Secret Access Key pair directly. This cost real debugging time tonight (`403 AccessDenied` with correct-looking permissions) before the root cause was found.

## Known Gaps / Deferred Items

- **`prisma/seed.ts`'s RP1–RP6 turnout/bring-in order is a guess.** The volunteer manual lists them as one block (`RP1–RP6`) without individual ordering; all six currently share the same order value. Fix once the real per-field walking order is known.
- **SSL deprecation warning** on every DB connection (`sslmode=require` being treated as an alias for `verify-full`, which will stop being true in a future major version of the `pg` driver). Harmless today, worth tightening to `sslmode=verify-full` explicitly before production.
- **Volunteer tier tenure timelines are approximate** (Green→Orange→Yellow "a few months / a year / another year") — get the real written schedule from Horse Haven before this becomes anything more than informational display.
- **R2's Public Development URL** should become a real custom domain before this goes live for actual volunteer use — see the Cloudflare R2 row above.
- **No Case File Export yet** — the legal-defensibility data (ChangeLog, append-only patterns) is all being captured correctly as features get built, but the actual export report UI is still a Phase 2 item per `CONTEXT.md` §15, pull forward if a real legal need becomes active.

## Medication + Care/Health Tracking — Built This Session

Both slices landed the same way feeding did: no schema changes needed (`MedicationRegimen`/`MedicationLog` and `CareEntry`/`CareType`/`HealthIssue` were already fully modeled in `prisma/schema.prisma` from the original grill session), just new Server Actions plus two new sections on `src/app/horses/[id]/page.tsx`.

- **Medication** (`src/app/horses/[id]/medication-actions.ts`): `createMedicationRegimen` is Admin-only, mirroring `FeedingBaseline`. `logMedicationAdministered` is Admin or Shift Lead, mirroring `FeedingOverride` — one log row per regimen per day, given/missed with notes. Added `endMedicationRegimen` (Admin-only, sets `endDate`) since, unlike `FeedingBaseline`, `MedicationRegimen` actually has an end date and regimens do run out — the horse detail page only lists regimens with no `endDate` or `endDate >= today`.
- **Care/Health** (`src/app/horses/[id]/care-actions.ts`): `createCareEntry` is Admin or Shift Lead — this one's explicit in `CLAUDE.md`'s permission table already. `createHealthIssue`/`resolveHealthIssue` aren't in that table (only `CareEntry`/`FeedingOverride`/`CheckIn` are listed for Shift Lead), so this was a judgment call: gave Shift Lead the same access as `CareEntry` itself, on the reasoning that opening/closing a health issue is just bookkeeping around the care entries they're already trusted to log, not a new category of authority. Flagging this so it can be corrected if that's wrong — worth a real answer from Lori/Ashley the way the other open items in `CONTEXT.md` §16 are tracked.
- **Fixed a `trackedModels` gap while in there:** `MedicationLog` wasn't in `src/lib/prisma.ts`'s `trackedModels` array even though it's the exact same "daily log entry" shape as `FeedingOverride`, which is tracked. `MedicationRegimen`, `CareEntry`, and `HealthIssue` were already in the array from the original schema session (pre-added, unused until now) — `MedicationLog` looks like it was just missed at the time, not a deliberate exclusion (there's no `CONTEXT.md`/`CLAUDE.md` reasoning for excluding it the way there is for `HorsePhoto`). Added it. `CareType` stays untracked, correctly — it's a lookup table like `FeedType`, not a data entity.
- Verified directly against the live Neon DB (see above) rather than through the browser — no `chromium-cli` or equivalent browser automation tool was available in this environment, and driving a real Clerk sign-in headlessly wasn't practical. `npx tsc --noEmit` and `npm run lint` both pass clean (matching what CI actually checks — there's no `next build` step in `.github/workflows/ci.yml`). **Still worth a manual click-through in a real browser before trusting this fully** — the query logic and write paths are verified, but the actual rendered UI (form layout, the "End regimen" link sitting inside the same table cell, etc.) has not been visually confirmed.
- No E2E coverage added, consistent with feeding (which also shipped without Playwright coverage) — `tests/e2e/` still only has the placeholder smoke test, and there's no Clerk-authenticated test fixture yet for any protected page. Worth setting up before this drifts further from `CLAUDE.md`'s stated testing priorities.

## Metrics + Field/Pasture Assignment — Built This Session

Same pattern as medication/care/health before it: no schema changes needed (`HorseMetric`/`WeightEntry`/`MetricType` and `Field`/`PastureAssignment` were already fully modeled from the original grill session, and `MetricType` + all 14 `Field` rows were already seeded), just new Server Actions plus two new sections on `src/app/horses/[id]/page.tsx`, plus one new top-level page.

- **Metrics** (`src/app/horses/[id]/metrics-actions.ts`): `createWeightEntry` and `createHorseMetric`, both Admin or Shift Lead. This permission split wasn't specified anywhere (`CLAUDE.md`'s permission table didn't cover `WeightEntry`/`HorseMetric` at all) — asked James directly this session rather than inferring it the way `MedicationLog`/`HealthIssue` access was inferred last session; he confirmed Admin-or-Shift-Lead, reasoning it the same way as `CareEntry` (a routine observation logged during a shift, e.g. at a weigh-in). Recorded in `CLAUDE.md`'s Permissions Quick Reference as a settled decision, not an open item. The horse detail page shows the last 10 `WeightEntry` rows and last 10 `HorseMetric` rows (joined to `MetricType` for name/unit) as two separate lists side by side — kept separate in the UI because they're deliberately separate tables in the schema (`CONTEXT.md` §13: `WeightEntry` is the timeline of record other things reference; `HorseMetric` is the generic bucket everything else, including Henneke BCS and height, grows into via new `MetricType` rows).
- **Field/Pasture** (`src/app/horses/[id]/pasture-actions.ts`): `assignPasture`, Admin-only per `CONTEXT.md` §10 (not a judgment call — explicit in the design doc already). Closes the horse's current active `PastureAssignment` (sets `endDate` to today) if one exists, then creates the new row — two sequential awaits, deliberately no `$transaction`, matching §10's explicit reasoning that this needs to stay fast/simple rather than fight Postgres range constraints. The horse detail page's new "Field / Pasture" section shows the current field, a short history, and the move form (Admin-only; everyone else just sees current + history read-only).
- **New `src/app/fields/page.tsx`**: the plain field list V1 calls for (`CONTEXT.md` §1/§10) standing in for the Phase 2 interactive drone-photo map. Read-only for any signed-in volunteer — lists all active fields with turnout/bring-in order and whichever horse(s) currently have an open assignment there. A field can hold more than one horse at once (it's a per-horse exclusivity rule, not a per-field one), so this renders as a list per row, not a single name. Linked from the homepage nav. Moving a horse still only happens from that horse's own detail page, kept as a single action location rather than duplicating the move form here too (asked James, he confirmed detail-page-only).
- Verified directly against the live Neon DB with a throwaway script (same style as the medication/care session): created a real `WeightEntry` + `HorseMetric`, read them back through the exact query shapes `page.tsx` uses, exercised the close-old/open-new `PastureAssignment` sequence including the fields-list join, then cleaned up via raw Prisma deletes/restores bypassing `withChangeLog` so no test noise landed in `ChangeLog` and the horse's real prior pasture assignment was restored exactly. `npx tsc --noEmit` and `npm run lint` both pass clean. **Still worth a manual click-through in a real browser before trusting the UI fully** — same caveat as last session, the actual rendered layout hasn't been visually confirmed.
- `trackedModels` in `src/lib/prisma.ts` already had `WeightEntry`, `HorseMetric`, and `PastureAssignment` from the original schema session — no gap to fix this time, unlike the `MedicationLog` miss found last session.

## Cross-Entity Daily Dashboard — Built This Session

The dashboard called out as item 1 in the prior session's "What's Next": one row per horse, feed/meds/care/metrics/pasture at a glance. No schema changes and no new Server Actions — everything it shows was already being written by the five prior feature slices, this is purely a new read path plus a new page.

- **`src/app/dashboard/page.tsx`**: lists all `ACTIVE` horses (matches the default filter `horses/page.tsx` already uses) with, per horse: current pasture (`PastureAssignment` with `endDate: null`), today's feeding (`FeedingBaseline` rows joined with today's `FeedingOverride` if one was logged, split AM/PM the same way the horse detail page shows it), today's medication status per active `MedicationRegimen` (given/missed/pending, derived from whether today's `MedicationLog` row exists and what it says), open `HealthIssue` rows plus the single most recent `CareEntry`, and the single most recent `WeightEntry`. Read-only — no forms, no writes; logging still happens from each horse's own detail page, this is purely the "what needs attention today" overview.
- **Access:** any signed-in volunteer (`requireVolunteer()`, no role check), matching the existing precedent set by `horses/page.tsx`, `horses/[id]/page.tsx`, and `fields/page.tsx` — none of those role-gate reads today, only writes are role-gated per the Permissions Quick Reference. Kept consistent rather than inventing a new, narrower read boundary for this one page.
- **Query approach:** one query per model across all active horses (`where: { horseId: { in: horseIds } }`), grouped into `Map`s in JS afterward, rather than one query per horse per model — stays a fixed number of round trips regardless of herd size. The one new pattern not used elsewhere in the codebase: "most recent `CareEntry`/`WeightEntry` per horse" uses `orderBy: [{ horseId: "asc" }, { date: "desc" }], distinct: ["horseId"]`, which Prisma compiles to Postgres `DISTINCT ON` — confirmed working against the live Neon DB via a throwaway `tsx` script (read-only, no writes, deleted after) before trusting it, since this specific query shape hadn't been exercised anywhere else in this project yet.
- Verified against live Neon data the same way as prior sessions (no browser automation available in this environment): the throwaway script above ran the dashboard's exact query shapes against the real 4-horse dataset and printed the grouped results per horse — feeding, medication, and weight all matched what's actually in the DB (e.g. Remus's AM/PM Strategy baselines, Aries's one weight entry). `npx tsc --noEmit` and `npm run lint` both pass clean. **Still worth a manual click-through in a real browser before trusting the UI fully** — table layout with several stacked `<ul>`s per cell hasn't been visually confirmed, same caveat as every prior slice.
- Homepage nav (`src/app/page.tsx`) got a `Dashboard` link alongside the existing `Check in`/`Horses`/`Fields`/`Admin check` links.

## Test Suite — Built This Session

This session was run via Claude Code directly against the repo (real local push/run access, no patch-file handoff), and closed out the testing gap every prior session's "What's Next" had been carrying forward. Full detail on the design is in `CLAUDE.md`'s Testing section; this is what actually happened.

- **Vitest (API/DB tests):** 81 tests across `tests/vitest/`, all passing against a real dockerized Postgres — `lib/auth.test.ts` (10), `lib/changelog.test.ts` (22, the `withChangeLog` extension itself), and one file per Server Action module plus the two Route Handlers (checkin, horses, feeding, medication, care, metrics, pasture, photos, clerk-webhook). Every permission boundary in `CLAUDE.md`'s Permissions Quick Reference has a test asserting the wrong role is rejected *and writes nothing*, not just that the right role succeeds.
- **Playwright (E2E):** 21 tests across 7 spec files, signed in via `@clerk/testing`'s real (ticket-based, no password) Clerk sign-in against three fixed test users (`e2e-admin@volunteer-ops.test` etc. — see `tests/e2e/test-users.ts`), covering check-in, horse CRUD, feeding baseline+override, pasture reassignment, medication/care/health/metrics logging, the dashboard, and role-based UI visibility. `tsc --noEmit`, `npm run lint`, and `npx playwright test --list` all pass clean — **but the suite has not actually been executed against live Clerk in this session.** First real run creates the three test users in your live Clerk dev instance (idempotent after that); this was deliberately left for James to run himself (`npm run test:e2e`, after `npm run test:db:reset`) rather than done automatically, since it's the one part of this session that reaches outside the local sandbox. Treat this the same as every prior session's "not visually confirmed in a real browser" caveat — the code is real and reviewed, but this specific execution hasn't happened yet.
- **Test database:** `docker-compose.test.yml`, a throwaway `postgres:16-alpine` on port 5433 (tmpfs-backed, no persistent volume — always starts clean). Both Vitest and Playwright refuse to run if `DATABASE_URL` doesn't contain `localhost`, specifically so a misconfigured env can never point a test run's `TRUNCATE` at Neon. `ci.yml` was rewritten to use a GitHub Actions `services:` Postgres container on the same port instead of Neon secrets — CI no longer has any path to touching production data at all, which is a meaningful safety improvement over the prior setup (the old `ci.yml` ran the placeholder E2E smoke test directly against real `secrets.DATABASE_URL`/`DIRECT_URL`).
- **New local dependency:** Docker, for `docker-compose.test.yml`. Confirmed available and working in this session's environment (Docker 29.5.3 / Compose v5.1.4). If a future machine doesn't have it, `npm run test:unit`/`test:e2e` will fail at the DB-connection step with a clear "connection refused," not something subtler.
- Quick start: `npm run test:db:reset && npm run test:unit` gets Vitest running from zero in well under a minute. `npm run test:e2e` additionally needs `.env` to have real, valid `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`/`CLERK_SECRET_KEY` (already does, per the Clerk row above) since E2E auth is real Clerk, not mocked.

## E2E Suite — Actually Run for the First Time (2026-07-18)

The prior session's "run `npm run test:e2e` for real, once" item turned out to hide two real bugs, not just an unexecuted step — the suite as committed could never have passed on any machine.

- **`test:e2e`'s dotenv-cli invocation never loaded `.env`.** `package.json` had `"test:e2e": "dotenv -e .env.test -- playwright test"` — only `.env.test`, which has no Clerk keys. `global-setup.ts` had a comment claiming the real command was `-e .env.test -e .env`, but that second flag was never actually in the script. Result: `clerkSetup()` failed immediately with "You need to set the CLERK_PUBLISHABLE_KEY environment variable," before creating any test users. CI's E2E step was unaffected by this specific bug (the workflow injects the Clerk keys directly via `env:` on that step, bypassing dotenv-cli), but would still fail today on missing repo secrets — see below. **Fixed:** script is now `dotenv -e .env.test -e .env -- playwright test`; confirmed the local test DB URL still wins over `.env`'s real Neon URL (dotenv-cli: first `-e` takes precedence on overlapping keys).
- **The `.test` TLD used for all three E2E test emails (`e2e-*@volunteer-ops.test`) is rejected by Clerk's live API.** RFC 2606 reserves `.test` for exactly this purpose, but Clerk's own email-format validator 422s on it (`form_param_format_invalid`) — confirmed by direct API probe. `.example.com` (also RFC 2606-reserved, guaranteed non-deliverable) is accepted. **Fixed:** `tests/e2e/test-users.ts` now uses `e2e-*@volunteer-ops.example.com`. The three real (disposable) Clerk test users have now been created in the live dev instance.
- **First real run result: 19/21 passing**, after also fixing two locator bugs surfaced by actually running the suite for the first time (`care-and-metrics.spec.ts`: a `getByText` match that hit both a list item and a `<select>` option — strict-mode violation — and a substring match on "Open health issue" that was also matching the always-visible "Open health issue**s**" read-only heading; both are test-only fixes, not app bugs, and both now use scoped/exact locators).
- **Both remaining failures root-caused and fixed same day.** Both turned out to be test bugs, not app bugs, and not related to each other despite looking like one flaky "isolation" problem at first:
  - **`fixtures.ts` used a bare `base.beforeEach()` call at module scope to truncate the DB before every test.** Node/ESM only evaluates a module once per process, so that call only ever executed the *first* time some spec file imported `fixtures.ts` within a given Playwright worker — Playwright attaches the hook to whichever file triggers that first import, and every other spec file sharing the same worker afterward runs with **no reset at all**, silently accumulating rows across tests. Confirmed by instrumenting `resetTransactionalData()` and the hook itself with a file-based trace log (console output isn't reliable here — Playwright's reporter suppresses stdout for passing tests): the hook fired for exactly one file per worker and never fired for the rest. This is a known category of Playwright gotcha — hooks declared in a shared helper module aren't reliably re-registered per file the way **fixtures** are (fixtures resolve per-test through Playwright's fixture graph regardless of module caching). **Fixed:** the truncate is now an auto fixture (`resetDb`, `{ auto: true }`) in `fixtures.ts`'s `test.extend()` instead of a `beforeEach` call. This was the actual cause of `horses.spec.ts`'s "a Shift Lead cannot create a horse" seeing a nonzero horse count — nothing to do with lock contention or connection pools, the DB just was never being cleared for that stretch of the run.
  - **`smoke.spec.ts`'s Admin/Volunteer role-check test requested both `adminPage` and `volunteerPage` fixtures in the same test.** Both depend on Playwright's single per-test `page` fixture (cached once per test), so the second fixture tried to sign in on the *same already-authenticated page* — hence `Clerk: Failed to sign in: You're already signed in`. Nothing to do with cross-test session bleed; it was self-contained within this one test. `fixtures.ts` already had `openAs(role)` built for exactly this "two roles active at once" case (used correctly elsewhere, e.g. `care-and-metrics.spec.ts`'s medication test) — this test just wasn't using it. **Fixed** by switching to `openAs("volunteer")` for the second actor.
- **Suite is now 21/21 passing**, confirmed on a clean `test:db:reset` + `test:e2e` run. Vitest's 81 tests re-confirmed passing too (no regression from the fixture refactor).

## V2 Session 1 Cleanup + Session 2: Tier Progression & Compliance Training (2026-07-18)

The prior V2.md Session 1 commit (`Location Model Generalization...`) had landed a real, working schema + Server Actions + a passing local commit, but this next session started by re-running the full suite cold and found it was **not actually green** — the commit's own last verification pass had missed several things. Fixed all of them before touching Session 2, per this file's usual "verify, don't assume the last session's summary" practice:

- **The Prisma client was stale** (schema had changed since the last `prisma generate`) — `npx prisma generate` fixed most of a long `tsc` error list immediately.
- **`Location.fieldCode` was still `NOT NULL` in the actual database.** The hand-written rename migration (`20260718230000_generalize_field_to_location`) renamed `Field.code` → `Location.fieldCode` but never dropped the `NOT NULL` constraint that column carried over from the horse-only-turnout-fields era — even though `schema.prisma` had always declared it nullable (required for `BARN_STALL`/`SICK_BAY`/`ARENA`/`OTHER` locations, which don't have a field code). This wasn't caught before because no test had actually tried to create a non-`FIELD` location yet. Fixed with a new migration (`20260718231500_location_fieldcode_nullable`), verified zero-drift via `prisma migrate diff` afterward.
- **`tests/vitest/actions/pasture.test.ts` and `tests/e2e/pasture.spec.ts` were still referencing the deleted `pasture-actions.ts` and the removed `PastureAssignment`/`Field` models** — they hadn't actually been migrated to `Location`/`AnimalLocationAssignment`, just left broken (`tsc` failed on both). Replaced with `tests/vitest/actions/location.test.ts` and `tests/e2e/locations.spec.ts`, covering everything V2.md Session 1's own test-coverage list asked for (`Location` CRUD+validation per type, append-only `AnimalLocationAssignment` with no update/delete path, current-location derivation per animal+period including DAY≠NIGHT, permission checks, full history). Extracted the derivation logic itself into `src/lib/locations.ts` (`getLocationHistory`/`currentFromHistory`) so it's exercised for real by both the animal detail page and the tests, rather than reimplemented in each.
- **`tests/e2e/dashboard.spec.ts` and `smoke.spec.ts` also still referenced the old `Field`/`PastureAssignment` models and the deleted "Fields" nav link** (now "Locations") — fixed both.
- **`CLAUDE.md`'s own Repo Layout section was describing files that no longer existed** (`pasture-actions.ts`, `fields/page.tsx`) and its Testing section still said `Field` instead of `Location` in a couple of places, plus the `withChangeLog` tracked-models list still named `PastureAssignment` even though the array in `src/lib/prisma.ts` had already dropped it. All fixed this session — see `CLAUDE.md` directly rather than duplicating the diff here.
- Result: `npm run test:db:reset && npm run test:unit` → **90/90 passing**, `npm run test:e2e` → **23/23 passing**, `tsc --noEmit`/`npm run lint` both clean, before Session 2 work started.

With Session 1 actually verified, built Session 2 (tier progression + compliance training) per `V2.md`:

- **Tier progression:** `Volunteer.firstShiftDate` (set once, from the first check-in's own date, never touched again — see `src/app/checkin/actions.ts`), `blueReleasedAt`/`blueReleasedById`, and a new `TierThreshold` table (admin-editable via `/tiers`, seeded GREEN/ORANGE/YELLOW/BLUE at 0/180/365/730 days). `src/lib/tier.ts`'s `computeTiers()` is pure and query-time-only, returning `computedEligibleTier` (capped below any manual-release tier) and `actualTier` (reaches BLUE only once tenure is met **and** released). **Confirmed directly with James** (V2.md flagged this exact question as genuinely ambiguous, not to be assumed): releasing a volunteer for Blue before they've hit the 730-day tenure threshold is **blocked outright**, not allowed-with-a-flag — see `src/app/volunteers/tier-actions.ts`'s `releaseBlue`.
- **Compliance training — a deliberate deviation from V2.md's literal spec, per this doc's own instruction to note such things:** rather than a new `TrainingRequirement`/`VolunteerTrainingCompletion` pair, extended the already-existing (but previously unused by any feature) `CredentialType`/`CredentialRecord` models — `CONTEXT.md` §7 already documented them as covering "vaccinations **and** training acknowledgments... under one mechanism," and the already-seeded `CredentialType` rows matched the exact domain V2.md wanted. Added `isRequired`/`renewalPeriodDays`/`appliesToTier`/`fileUrl` to `CredentialType`; `CredentialRecord.expiresAt` is now computed once at write time from `completedDate + renewalPeriodDays`. Self-attestation only (any signed-in volunteer, for themselves only — see `src/app/volunteers/training-actions.ts`), matching V2.md's "MVP" framing exactly. `src/lib/training.ts`'s `getMissingOrExpiredRequiredTraining()` powers a report on the new `/training` page.
- **New pages:** `/volunteers` (directory, tier column), `/volunteers/[id]` (tenure/tier detail, Blue-release form, credential/training status + self-attestation), `/tiers` (threshold edit), `/training` (`CredentialType` admin management + the missing/expired report).
- **A real, unrelated bug surfaced and fixed along the way:** `CredentialType.name` had **no unique constraint at all**, so `db:seed`'s `createMany({ skipDuplicates: true })` had nothing to dedupe against — every re-run silently inserted fresh duplicates. The long-lived local test container (never recreated across several sessions) had accumulated **16 duplicate copies** of every seeded `CredentialType`/`FeedType`/`CareType`/`WorkType`/`MetricType` row before this was noticed (via the new training report returning obviously-wrong duplicate-heavy results). Fixed for `CredentialType` specifically (added `@unique` on `name` via migration `20260719020000_credentialtype_name_unique`, with a defensive step reassigning any `CredentialRecord` FKs off duplicates before dropping them; switched seeding to a real `upsert`, matching `Location`'s existing pattern) and confirmed via a full `docker compose down -v` + fresh reset that the local test DB is clean again. **`FeedType`/`CareType`/`WorkType`/`MetricType` have the identical latent gap** — out of scope for this session (Session 2 doesn't touch them), but worth the same fix before `db:seed` gets run against any of them repeatedly, especially against live Neon.
- **Result:** `npm run test:db:reset && npm run test:unit` → **123/123 passing**, `npm run test:e2e` → **31/31 passing** (8 new tests in `tests/e2e/tier-and-training.spec.ts`), `tsc --noEmit`/`npm run lint` both clean. Verified in a real browser via Playwright's own `webServer` (a real `next build && next start` bound to the local test DB, not a mock) — no separate manual click-through was done against `.env`'s real Neon/Clerk, deliberately, per the note below.
- **Not applied to live Neon** — same holding pattern as the `Horse`→`Animal` rename before it (see the item below): this session never pointed anything at `.env`'s real `DATABASE_URL`/`DIRECT_URL`, only at the local Docker test database. Four migrations are now queued for Neon whenever James gives the go-ahead (see "What's Next").
- **No E2E coverage gap left over from Session 1** — everything V2.md Session 1's test-coverage list and Session 2's test-coverage list asked for now has both Vitest and Playwright coverage; see the two new spec files for the exact breakdown.

## V2 Session 3: Volunteer Tags & Go Team (2026-07-19)

Session 2's work (see above) was still sitting uncommitted in the working tree when this session started — confirmed it was actually solid before building on top, per this file's usual "verify, don't assume the last session's summary" practice: re-ran `npm run test:db:reset && npm run test:unit` (123/123) and `npm run test:e2e` (31/31) cold, both green, before touching anything new.

- **Schema:** `VolunteerTag` (id, `name` unique, `description`, `minDaysSinceBlueRelease` nullable, `active`) — a generic, extensible tagging lookup table, same admin-managed category as `Location`/`CredentialType`/`TierThreshold`, not ChangeLog-tracked. `VolunteerTagAssignment` (`volunteerId`, `tagId`, `assignedById`, `assignedAt`, `removedAt`/`removedById` nullable) — removal is a soft update, not a delete, matching `RegularShiftAssignment`'s close-don't-delete pattern. **Is** ChangeLog-tracked (added to `trackedModels` in `src/lib/prisma.ts`), unlike `AnimalLocationAssignment` — the distinction: `AnimalLocationAssignment` is pure-append (never updated after creation, so there's nothing for ChangeLog to diff), while `VolunteerTagAssignment` genuinely gets updated later (the removal itself), which is exactly the case ChangeLog exists for. Migration `20260719105907_volunteer_tags_and_go_team`, generated via `prisma migrate dev` pointed at the local Docker test database only (never Neon) — verified applying cleanly from a full `docker compose down -v` + fresh `test:db:reset` before trusting it.
- **The eligibility-threshold design decision:** V2.md asked for "a simple admin-editable config value" for the Go Team tenure-since-release requirement, rather than hardcoding a number. Instead of a new singleton settings table, added `minDaysSinceBlueRelease` directly as a nullable field on `VolunteerTag` itself — admin-editable via `/tags` like any other row edit, and it generalizes for free to any future tag that wants its own eligibility-candidates report (most won't set it, and get no report section). No deviation flagged here since this is additive to the schema V2.md described, not a replacement of a differently-named model (unlike Session 2's `CredentialType` extension, which *was* a real deviation).
- **`src/lib/tags.ts`**: `getTagEligibilityCandidates(tagId)` — Blue-released (`actualTier === "BLUE"`, computed via the existing `src/lib/tier.ts`) volunteers who've cleared the tag's `minDaysSinceBlueRelease` bar, excluding anyone who already actively holds the tag (this is a *candidates* report — a human decides who to actually assign, not a member list). Read-only, never assigns anything itself, per V2.md's explicit "report/list for a human to review, not an automatic tag assignment" instruction.
- **`src/app/volunteers/tag-actions.ts`**: `createVolunteerTag`/`updateVolunteerTag` are Admin-only (defining the tag *type*); `assignTag`/`removeTag` are Admin-or-Shift-Lead (applying it to a volunteer) — same type-vs-assignment split CLAUDE.md already documents for `Location`. `assignTag` rejects a duplicate *active* assignment (app-side check, not a DB constraint, matching `Location`'s own precedent for admin-entered data) but allows holding several different tags at once, and allows reassigning after an earlier assignment was removed (a fresh row).
- **New pages:** `/tags` (Admin manages tag types + edits the eligibility threshold; Admin/Shift-Lead additionally see each configured tag's eligibility-candidates list). `/volunteers/[id]/page.tsx` grew a "Tags" section — currently-held tags with a Remove button, plus an assign dropdown scoped to not-yet-held tags, both gated the same as the rest of that page's write affordances. Homepage nav got a `Tags` link.
- **Seed:** `Go Team` upserted with a placeholder `minDaysSinceBlueRelease: 180` — same category of "starting guess, not a commitment" as the RP1–RP6 turnout order, since the real Go Team tenure requirement isn't settled (`CONTEXT.md` §16).
- **Tests:** `tests/vitest/actions/tag.test.ts` (tag CRUD permission gating, assign/remove including the duplicate-rejection and multi-tag-holding cases, soft-remove-not-delete, reassignment after removal) and `tests/vitest/lib/tags.test.ts` (the eligibility-candidates query: threshold boundaries, non-Blue exclusion, already-tagged exclusion, re-inclusion after removal). `tests/e2e/volunteer-tags.spec.ts` (5 tests: Admin tag creation, Shift-Lead read-only tag list, Shift-Lead assign+remove round trip, Volunteer can't see assign/remove controls on someone else's page, the Go Team candidates report gaining/losing an entry as a volunteer crosses the threshold and gets tagged). Two E2E locator bugs found and fixed the same way prior sessions did (strict-mode violations from a tag's name matching more than one element on `/tags` once it also has an eligibility-candidates section) — test-only fixes, not app bugs, same category as the `care-and-metrics.spec.ts`/`smoke.spec.ts` fixes from the original E2E hardening session.
- **Result:** `npm run test:db:reset && npm run test:unit` → **143/143 passing**, `npm run test:e2e` → **36/36 passing** (5 new), `tsc --noEmit`/`npm run lint` both clean, confirmed on a from-scratch `docker compose down -v` reset, not just an incremental run.
- **Not applied to live Neon** — same holding pattern as every V2 migration before it; see "What's Next" below, now five migrations deep.
- **Per `V2.md`'s own rule, stopping here — do not start Session 4 (Event Scheduler & Signup) until James starts a new session for it.**

## What's Next

1. **Apply all queued migrations to live Neon, in order, as one coordinated cutover** — none of them have been run against production yet. This list had gone stale across several sessions (each new session added its own migration but didn't update this bullet) — it's now sixteen migrations deep, not the seven listed here previously. The ones this specific list had been tracking:
   - `20260718223633_rename_horse_to_animal` (Horse→Animal rename + Binx species backfill — held pending explicit go-ahead since the original rename session, still true)
   - `20260718230000_generalize_field_to_location` (Field/PastureAssignment → Location/AnimalLocationAssignment)
   - `20260718231500_location_fieldcode_nullable` (fixes the `NOT NULL` gap described above)
   - `20260719011730_tier_progression_and_training` + `20260719020000_credentialtype_name_unique` (V2.md Session 2)
   - `20260719105907_volunteer_tags_and_go_team` (V2.md Session 3)
   - `20260719113145_event_scheduler_and_signup` (V2.md Session 4)
   - `20260719140000_shift_templates_seasonal_hours_and_kiosk` (V2.md Session 5 — includes a live backfill of `Volunteer.checkInCode` via `gen_random_uuid()` for every existing row; re-read that migration's header comment before running it against real Neon data, same as the `Location.fieldCode` precedent)

   Plus everything added since, never folded into this list: `20260719150000_add_herd_order` (V2.md Session 6), `20260720114911_v3_session1_intake_groups_lineage_placement_group`, `20260720123412_v3_session2_recurring_facility_tasks`, `20260720143257_v3_session3_shift_alerts_announcements`, `20260720161338_v3_session4_shift_assigned_lead`. Run `ls prisma/migrations` for the authoritative current list rather than trusting this bullet again — it will keep going stale unless whoever runs the cutover updates it in the same commit.

   The deployed app code needs to move to the post-rename, post-Location routes/models at the same time — none of these are backwards-compatible with what's currently live.
2. Add `CLERK_PUBLISHABLE_KEY`/`CLERK_SECRET_KEY` as GitHub Actions repo secrets so CI's E2E step goes green instead of failing on missing secrets (`DATABASE_URL`/`DIRECT_URL` secrets are no longer needed by CI at all — see the External Services table). Not verified from this session — no `gh` CLI access to check current repo secrets state.
3. `nightly-backup.yml`'s Neon/R2 secrets still aren't configured (see the table above) — needed before the automated backup actually runs.
4. Fix the same `db:seed` non-idempotency gap in `FeedType`/`CareType`/`WorkType`/`MetricType` that was just fixed for `CredentialType` (add `@unique` on `name`, switch to `upsert`) — before `db:seed` gets run against Neon multiple times.
5. ~~Fill in a real `RESEND_API_KEY` in `.env`~~ **Done** — James added a real key right after this session flagged it as blank; confirmed working against the live Resend API (see the External Services table). `EMAIL_FROM` is still unset (sends from Resend's shared sandbox address) — add a verified `horsehaventn.org` sender before this is relied on for real.
6. A physical kiosk tablet at the barn still needs to actually be set up (mounted, browser pinned to `/kiosk`, probably kiosk-mode Chrome) and volunteers need their `checkInCode` badges/QR codes printed — V2.md Session 5 built the software path end to end but didn't touch physical deployment.
7. Per `V2.md`'s own rule, **stop here and wait for James to start a new session before beginning Session 6** (Dashboards: Feed Board & Turnout Board).

## Quick-Start Checklist for a New Session

```
git pull
npm install
npm run dev
```

To run the test suite (needs Docker running):
```
cp .env.test.example .env.test   # first time only
npm run test:db:reset            # brings up the throwaway test Postgres, migrates, seeds
npm run test:unit                # Vitest — API/DB tests, no external services touched
npm run test:e2e                 # Playwright — real Clerk sign-in, needs .env's Clerk keys
```

If you need to test anything involving new Clerk signups or webhook-dependent flows (not needed for most day-to-day feature work — only matters if the feature itself touches user creation):
```
ngrok http 3000
```
then update the endpoint URL (not create a new endpoint) in Clerk's dashboard to match the new ngrok URL.

If `npm run db:migrate`, `npm run db:seed`, or opening Prisma Studio fails on the first try with a connection error — that's almost certainly the Neon cold-start pattern, not a real problem. Just try again.
